// Public surface of the sequences feature slice — the destination component the (shell)/sequences route
// renders. The destination composes the slice's three tabbed surfaces (list/builder/enrollment · templates ·
// send status) internally; the route only needs SequencesPage.
export { SequencesPage } from "./components/SequencesPage";
