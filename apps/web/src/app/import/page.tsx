// page.tsx — thin route for the import destination. Renders the import feature's public component; all
// logic lives in the slice (apps/web/src/features/import).
import { ImportPage } from "@/features/import";

export default function Page() {
  return <ImportPage />;
}
