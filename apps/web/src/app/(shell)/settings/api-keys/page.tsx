// Settings ▸ Developer ▸ API keys — placeholder until the S5 (Developer settings) unit lands.
import { SettingsPlaceholder } from "@/features/settings-shell";

export default function ApiKeysSettingsRoute() {
  return <SettingsPlaceholder title="API keys" />;
}
