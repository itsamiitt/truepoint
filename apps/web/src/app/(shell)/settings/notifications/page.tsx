// Settings ▸ User ▸ Notifications — placeholder until the S1 (User settings) unit lands.
import { SettingsPlaceholder } from "@/features/settings-shell";

export default function NotificationSettingsRoute() {
  return <SettingsPlaceholder title="Notifications" />;
}
