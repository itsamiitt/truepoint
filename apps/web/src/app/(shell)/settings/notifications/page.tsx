// Settings ▸ User ▸ Notifications — mounts the settings-user slice's Notifications panel (12 §2).
import { NotificationsPanel } from "@/features/settings-user";

export default function NotificationSettingsRoute() {
  return <NotificationsPanel />;
}
