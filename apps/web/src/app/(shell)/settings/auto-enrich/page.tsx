// Settings ▸ Workspace ▸ Auto-enrich — mounts the auto-enrich policy panel (settings-enrichment slice, G-ENR-1).
import { AutoEnrichPanel } from "@/features/settings-enrichment";

export default function AutoEnrichSettingsRoute() {
  return <AutoEnrichPanel />;
}
