// Settings ▸ Workspace ▸ Teams — mounts the teams grouping panel (settings-teams slice, Part D).
import { TeamsPanel } from "@/features/settings-teams";

export default function TeamsSettingsRoute() {
  return <TeamsPanel />;
}
