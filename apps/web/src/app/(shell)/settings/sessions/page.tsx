// Settings ▸ Workspace ▸ Sessions — mounts the active-sessions admin panel (settings-workspace slice, G-AUTH-2).
import { SessionsPanel } from "@/features/settings-workspace";

export default function SessionsSettingsRoute() {
  return <SessionsPanel />;
}
