// (shell)/companies/page.tsx — the Companies destination index (market-intelligence MI-1). Thin: all
// behavior lives in the feature slice (features/companies).
import { CompaniesIndexPage } from "@/features/companies";

export default function CompaniesRoute() {
  return <CompaniesIndexPage />;
}
