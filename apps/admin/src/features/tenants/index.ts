// Public surface of the Tenants area (13 §3.1) — the directory + detail pages the App Router routes mount.
export { TenantsPage } from "./components/TenantsPage";
export { TenantDetailPage } from "./components/TenantDetailPage";
