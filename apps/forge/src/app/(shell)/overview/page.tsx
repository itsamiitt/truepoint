// /overview — thin mount of the Overview feature Page (the operator dashboard). The (shell) layout supplies the
// chrome + the two-stage auth/staff gate.
import { OverviewPage } from "@/features/overview";

export default function Page() {
  return <OverviewPage />;
}
