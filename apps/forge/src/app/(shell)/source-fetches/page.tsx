// /source-fetches — thin mount of the Source fetches feature Page. The (shell) layout supplies the chrome +
// auth/staff gate.
import { SourceFetchesPage } from "@/features/source-fetches";

export default function Page() {
  return <SourceFetchesPage />;
}
