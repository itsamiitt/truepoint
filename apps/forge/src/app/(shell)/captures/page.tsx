// /captures — thin mount of the Captures feature Page. The (shell) layout supplies the chrome + auth/staff gate.
import { CapturesPage } from "@/features/captures";

export default function Page() {
  return <CapturesPage />;
}
