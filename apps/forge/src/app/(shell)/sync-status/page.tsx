// /sync-status — thin mount of the Sync status feature Page. The (shell) layout supplies the chrome + gate.
import { SyncStatusPage } from "@/features/sync-status";

export default function Page() {
  return <SyncStatusPage />;
}
