// /review — thin mount of the Review feature Page. The (shell) layout supplies the chrome + auth/staff gate.
import { ReviewPage } from "@/features/review";

export default function Page() {
  return <ReviewPage />;
}
