// /parsers — thin mount of the Parsers feature Page. The (shell) layout supplies the chrome + auth/staff gate.
import { ParsersPage } from "@/features/parsers";

export default function Page() {
  return <ParsersPage />;
}
