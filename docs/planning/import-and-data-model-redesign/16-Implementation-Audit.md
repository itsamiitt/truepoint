# 16 — Implementation Audit (living document)

> **Purpose:** the single living record of what from this series has actually shipped, in the
> pattern of [`database-management-research/16-Implementation-Audit.md`](../database-management-research/16-Implementation-Audit.md).
> Everything in this series is **design-only** until a row here says otherwise.
>
> **Update protocol:** badge changes flow `16` → `01` (status column) → the gap row in `02` →
> the phase gate in `14`. A doc is never edited to *pretend* something shipped; this file is
> the only place shipped-status lives.

Legend: ✅ shipped & live · 🌒 built, dark (flag-off) · 💤 built, inert/double-gated ·
🟡 partial · 🔲 not built (design only) · ❌ blocked on missing prerequisite.

## Subsystem status

| Area | Design doc | Status | Evidence (file:line / commit) |
|---|---|---|---|
| Job-visibility fix (import/reveal/enrichment lists + RecentImportsCard) | `10` | 🌒 built, dark (S-V1–S-V4; dual gate off/off; CI gates owed before any flip) | commits `71615db` (S-V1 DDL), `8ee6901` (S-V2 predicate/renames), `a69ebe2` (S-V3 wiring + T-V1–T-V4), `97f8a8c` (S-V4 grants/policy + T-V5/T-V6); adversarial review PASS 2026-07-05 |
| Dead-end "Large file" toggle kill + `/imports` scaffold + TanStack seam (S-U1) | `11` | ✅ built (flag-independent; ships with next deploy; lockfile/`next build` CI-owed) | commit `77d2182`; grep gate `largeFile`/`act-bulk-import` extinct |
| Durable sync-import fast path (small files on `import_jobs` trio) | `08` | 🌒 built, dark (S-I3; `IMPORT_V2_ENABLED` dual gate off/off; T1/T4/T5 itests CI-owed) | `packages/core/src/import/runFastImport.ts`, `apps/api/src/features/import/{routes.ts (POST/GET forks), importV2Gate.ts, bulkQueue.ts (enqueueFastImport)}`, worker `register.ts` + `bulkImports.ts` fast kind; `apps/workers/test/importFast.parity.itest.ts` |
| Tenant import surface — history list / detail / cancel / retry-failed + server-side routing + strategy triad + published limits | `08`/`10`/`12` | 🌒 built, dark (S-I4/S-I5/S-I6/S-I10 ride `IMPORT_V2_ENABLED` off/off; S-P2 constants live; T2/T3/T9/T6/T7/T10/TP-7 itests CI-owed) | commits `003ee71` (S-I4 keyset list + additive v2 detail + cancel + `IllegalStateError`), `63ceffa` (S-I4 fast-cancel boundary), `22fa982` (S-P2 `importLimits.ts` + `ImportTooLargeError`), `4a82f85` (S-I5 routing pre-gate), `ebdca16` (S-I6 triad via `planFieldWrite` + legacy mapping), `5f0aeb1` (S-I10 retry-failed child + commit quota + deferred shed + `ImportQuotaExceededError`); `apps/api/src/features/import/routes.ts`, `packages/types/src/{importLimits.ts,errors.ts}`, `packages/core/src/import/{runImport.ts,runFastImport.ts,admission.ts}`, `packages/db/src/repositories/importJobRepository.ts (listJobs keyset + getJobForUpdate + countJobsCreatedSince + listRetryableRows)` |
| Import history UI (dedicated Imports section) | `11` | 🌒 **built, dark — S-U2..S-U7 all shipped** (S-U2 dashboard+drawer+Cancel · S-U3 durable job page + hand-off + give-up deleted [G11] · S-U4 auto-map + merge-mode triad + preserve switch · S-U5 completion notifications deep-link to the job · S-U6 reject-histogram + artifact downloads + retry-failed dialog · S-U7 draft-flow wizard: upload-once server draft + server auto-map proposal + PUT-on-advance + full-pass preview + Idempotency-Key commit + `?step`/`?draft` deep-link/resume + history drafts banner w/ discard — **canary with S-I8**, gate-off = today's one-shot card byte-identical). Degrades to "not enabled" while `IMPORT_V2` off | `66fc684` (S-U2), `489c188` (S-U3), S-U4 (wizard strategy+auto-map), S-U5 (notification deep-links), S-U6 (`ImportJobPage` error UX), `142809e` (S-U7 draft wizard) + banner/tests commit; `apps/web/src/features/import/{draftFlow.ts,apiDrafts.ts,hooks/useImportDraft.ts,hooks/useImportDrafts.ts,components/ImportDraftFlow.tsx}` |
| Bulk pipeline enable (COPY path live) | `08`/`14` | 🌒 **built, dark — S-I9 CODE half ✅** (15 seq 40): unified routing engagement — over-threshold + the graduated `BULK_IMPORT_ENABLED`+`bulk_import_enabled` pair on (evaluated INSIDE the IMPORT_V2 gate; NO new flag) ⇒ the one-shot POST and the draft commit route to `processing_mode='copy'` via core's ONE `submitCopyImport` (store object → enqueue drive) instead of refusing; either gate off ⇒ the honest `file_too_large` ceiling, byte-identical (§R-P2). `/imports/bulk` = thin delegate over the same helper (RFC 9745 `Deprecation` + successor `Link` headers; its rows now marked `processing_mode='copy'` + `source_filename`). 08 §2.4 legacy mapping verified total over the 12 states (copy `staged → active`), unit-tested. **LIVE engagement ❌ user/CI-owed:** pair stays off/off until A+B+C green + the ≥100k-row real-file canary E2E + idempotency replay (14 exit) | `packages/core/src/import/{routing.ts(+.test.ts),submitCopyImport.ts}`, `apps/api/src/features/import/{routes.ts,bulkRoutes.ts,importV2Gate.ts (isCopyModeEngaged),legacyStatus.ts(+.test.ts)}`, `packages/db/test/importCopyRouting.itest.ts`, migration `0057` (P2 audit CHECK), worker `register.ts` (unchanged — copy kinds already consume gate-on) |
| Import error artifacts (repair CSV + error report, typed codes, gated proxied download) | `08`/`10`/`13` | 🌒 built, dark (S-I7/S-S3/S-V5/S-S4; `IMPORT_V2_ENABLED` off; unit T-S1/T-S5 landed, T-V7/T-S4 itests CI-owed) | commits `4ae237f` (S-I7 pair + typed codes + ledger token + write-back), `0e5adf6` (S-S3 neutralizer + `_REDACTED_`), `fb80de8` (S-V5 gate + audit); `packages/core/src/import/artifactWriter.ts`, `packages/types/src/importReject.ts`, `apps/api/src/features/import/artifactRoutes.ts` |
| Artifact lifecycle (TTL sweep + key-nulling + job-purge prefix seam + `import_artifacts` class) | `13` §4.4 (S-S7) | 🌒 built, dark (rides the unified-queue construction gate; artifacts only exist gate-on; TTL/cadence revert by env; sweep-decider unit landed, e2e itest CI-owed) | `apps/workers/src/queues/importArtifactSweep.ts` (+ `.test.ts`), `packages/core/src/import/artifactLifecycle.ts` (`purgeImportJobObjects` — the hard-purge seam for the future row-purger/S-S8), `importJobRepository.{listArtifactExpiryCandidates,clearArtifactKeys}`, env `IMPORT_ARTIFACT_TTL_DAYS`(90)/`IMPORT_ARTIFACT_SWEEP_EVERY_MS`, types `retentionDataClass` += `import_artifacts`, runbook §K row |
| Draft flow (upload-once → `PUT mapping` → full-pass preview → commit + auto-map alias table + draft reaper + cancel-discard) | `08` §2–§4 (S-I8) | 🌒 built, dark (rides `IMPORT_V2_ENABLED` off/off; **canary-only until Gate C** per 15 row 35 — works TODAY on `diskFileStore`, uses the S3 adapter transparently when the Gate-B env lands; bucket provisioning stays user-owed; T11/T12 itests + alias/reaper units CI-owed) | commits `a3921e4` (draft verbs: POST /imports no-mapping dispatch, PUT `/:id/mapping`, POST `/:id/preview`, POST `/:id/commit`; core `headerAliases.ts` + `draftPreview.ts`; repo draft-pinned writes + list draft filter), `9e22e9a` (reaper job 4 + `IMPORT_DRAFT_TTL_HOURS`(48 h) + cancel-from-draft discard), tests `packages/db/test/importDraft.itest.ts` + `headerAliases.test.ts` + reaper-decider units |
| `contact_emails` / `contact_phones` child tables | `05` | 🌒 **S-CH1 DDL landed, inert — dead schema** (15 seq 44): both tables per 05 §1–§2 (the §2.2 email-ws-unique / phone-contact-unique asymmetry + `idx_contact_phones_ws_e164` signal probe + `uniq_*_primary` + fetch/facet composites), contacts-shaped FORCE-RLS + triggers + grants, 0056's autovacuum params re-stated (S-P5 tripwire closed), retention classes seeded (null/shadow), P3 `channel_*` audit-CHECK (ruling M1), channel DTO vocabulary + `phoneLineType` union widening in types. **Nothing reads or writes the tables until S-CH2 (`applyChannelWrite` + `CHANNEL_DUAL_WRITE`)**; T-P3 RLS itests ×2 + drizzle regen CI-owed | migration `0058_contact_channels.sql`, `packages/db/src/schema/contactChannels.ts`, `packages/db/src/rls/contactChannels.sql`, `packages/types/src/contactChannels.ts`, types `retention.ts`/`billing.ts`/`contacts.ts` (phoneLineType) |
| Channel dual-write (`applyChannelWrite` + every email/phone writer instrumented) | `05` | 🌒 **S-CH2 built, dark** (15 seq 45; dual gate `CHANNEL_DUAL_WRITE` env + `channels_dual_write` per-tenant flag, off/off — either off ⇒ every writer byte-identical AND cost-identical, zero flag reads on the env short-circuit): `contactChannelRepository.applyChannelWrite` is CH-INV-1's single sanctioned write path (per-contact dedup on blind_index; first live value ⇒ primary + flat projection from the op's bytes; existing primary NEVER flipped; email ws-unique collision ⇒ `collision` outcome, never an error; per-contact cap 25 ⇒ `capped`; primary byte-refresh on dedup hits keeps the projection byte-exact through re-encrypting re-imports; verify ops mirror grades onto the live primary). Instrumented writers, all same-tx as their flat write: `runImport` (sync + the fast wrapper; once-per-run gate eval; landing rows only; import:src + source_import lineage), `enrichContact` (provider fills; secondaries appended, the 02 §RC-4 fix), `revealContact` + `runReverification` (verify-mirror ops). E.164 via the shipped `toE164` (DM1, zero new normalizers); unparseable kept raw + NULL e164 material. NOT instrumented (drift rows below): the dark bulk COPY chunk merge; `editContactFields` (scalar-only — no shipped user channel surface). §R-P3: flag off ⇒ writers revert to flat-only instantly, child rows stay inert. T-CH parity itest + CH-INV-1/collision/shared-phone/cap/verify itests + pure-decision units authored; **bun test/typecheck/biome CI-owed**; S-CH3 (backfill) next, then re-run to close the write-gap tail | migration `0059_channels_dual_write_flag.sql`, env `CHANNEL_DUAL_WRITE`, types `CHANNELS_DUAL_WRITE_FLAG_KEY`, `packages/db/src/repositories/{contactChannelRepository.ts,contactChannelPlan.ts(+.test.ts)}`, `packages/core/src/channels/channelDualWrite.ts(+.test.ts)`, writers `packages/core/src/{import/runImport.ts,import/prepareContact.ts,enrichment/enrichContact.ts,reveal/revealContact.ts,data-health/reverifyContacts.ts}`, `packages/db/test/contactChannels.dualwrite.itest.ts` |
| Channel backfill (flat → primary child rows) | `05`/`15 §2.1` | 🌒 **S-CH3 built, dark** (15 seq 46; registered only when `CHANNEL_DUAL_WRITE` AND the new `CHANNEL_BACKFILL_ENABLED` both read `"true"` — dark by default, nothing built when off): leader-locked, self-terminating sweep (the ledgerBackfillSweep one-shot idiom) — owner-conn census (non-PII ids) → per workspace `withTenantTx` keyset batches (1k default, ONE tx per batch, RLS ENFORCING — never the owner conn for writes, rule 7) → per contact: email child from **VERBATIM flat ciphertext + blind index (never decrypted — CH-INV-1 by construction)**, phone child decrypt→`toE164` in-worker (core `decryptPii` + the S-CH2 `buildPhoneChannelValue`; hint from ISO-2 `locationCountry`; unparseable ⇒ e164 NULL, kept raw, flagged, never skipped); statuses/line_type mirrored flat-wins; `is_primary=true`, `source='backfill'`, `type='other'`, pinned=false. Watermark = the WHERE-missing selection itself (drift row); abort = the dual gate re-evaluated in-tx at every batch boundary (drift row); per-contact all-or-nothing via the batch tx (an injected mid-batch failure rolls the whole batch back — itest-proven); never touches existing child rows (dual-write-then-backfill collision-safe in any order, `ON CONFLICT DO NOTHING` race backstop). Dedicated repo entry `backfillContactChannels` (15 §2-sanctioned; `applyChannelWrite` deliberately NOT reused — it re-projects flat + byte-refreshes primaries ⇒ fleet-wide `updated_at` churn + non-no-op re-runs). **`countContactsMissingChannelProjection` = THE S-CH4 gate** (15 §2.1 predicate shared verbatim by selection/census/count; exposed as the `leadwolf_channel_backfill_remaining` gauge). Idempotency/resume/collision/gate/atomicity/completeness itests + pure batch-decider units authored; **bun test/typecheck/biome CI-owed**; live sequencing: enable AFTER `channels_dual_write` is on for the tenant cohort, re-run to close the write-gap tail, completeness = 0 + drift = 0 before S-CH4 | env `CHANNEL_BACKFILL_ENABLED`/`CHANNEL_BACKFILL_BATCH_SIZE`/`CHANNEL_BACKFILL_BATCHES_PER_TICK`, `packages/core/src/channels/channelBackfill.ts(+.test.ts)`, `contactChannelRepository.{findContactsMissingChannelProjection,backfillContactChannels,listWorkspacesMissingChannelProjection,countContactsMissingChannelProjection}`, `apps/workers/src/queues/channelBackfillSweep.ts`, `apps/workers/src/{register.ts,metrics.ts}` (`leadwolf_channel_*`), `packages/db/test/contactChannels.backfill.itest.ts` |
| Channel read cutover (masked summaries · live-child presence · dedup email rung · reveal/export primary-first) | `05` | 🌒 **S-CH4 built, dark** (15 seq 47; composed read gate `CHANNEL_READ_FROM_CHILD` env + `channels_read` per-tenant flag, IMPLYING the S-CH2 dual gate — read implies dual-write, fail-closed at every layer; off/off): every cut-over read surface consults the ONE evaluator (`isChannelReadFromChildEnabled` / `channelReadFromChildEnabledForScope`) and takes an `opts.channelsFromChild` the CALLER evaluates (repos never read flags). **Gate-off EVERY surface is byte-identical with ZERO extra child reads (env short-circuit ⇒ zero flag queries).** Cut over gate-on: (a) the masked SearchPort projection (`searchRepository`) — has_email/has_phone (filter + projection), `complete`, and the `company` email-domain leg resolve from live child rows (secondaries count, G16), and each hit gains masked `channels` summaries via ONE batched query per table per page (no N+1); facet COUNT grouping stays flat (drift). Wired at `searchPortProvider` (gate once per port). (b) `contactRepository` masked list/detail (`listByWorkspace`/`listMaskedByIds`) — `channels` + has_email/has_phone/dataHealth from live child counts, batched. (c) the dedup email rung (`findByDedupKeys`/`findByDedupKeysBatch`) retargets to `contact_emails.blind_index` (secondaries resolve; §2.2 ws-unique keeps ≤1 hit/key, one query/chunk) — wired in `runImport` (once per run). (d) `getRevealedContact` (single+batch) — `emails`/`phones` arrays primary-first from child, decrypted only for owned claims, batched. (e) `bulkRevealExport` — additive `additionalEmails`/`additionalPhones` columns (secondaries; primary already in email/phone by CH-INV-1). The flat columns REMAIN the permanent primary cache (05 §3.4). **FLIP PRECONDITIONS (05 §Rollout / 15 §T-P3): S-CH3 completeness = 0 (`countContactsMissingChannelProjection`) + drift = 0 + T-P3 parity itests green in CI.** T-CH-R read-cutover itests (masked non-leak · gate-off byte-identity · dedup-rung parity · reveal primary-first · facet parity) authored; **bun test/typecheck/biome CI-owed** | migration `0060_channels_read_flag.sql`, env `CHANNEL_READ_FROM_CHILD`, types `CHANNELS_READ_FLAG_KEY` + `contactChannelSummariesSchema`/`revealedEmailValueSchema`/`revealedPhoneValueSchema` (contacts.ts/billing.ts), `packages/core/src/channels/channelRead.ts`, `packages/db/src/repositories/{contactChannelRepository.ts (read family, fc3956e),searchRepository.ts,contactRepository.ts}`, `packages/core/src/{import/runImport.ts,reveal/getRevealedContact.ts,reveal/bulkRevealExport.ts}`, `apps/api/src/features/search/searchPortProvider.ts`, `packages/db/test/contactChannels.readcutover.itest.ts`; commits `fc3956e` (grain 1: gate + read repo), `5a1c481` (read cutover) |
| Channel reconcile / drift sweep (permanent CH-INV-1 fixture) | `05` | 🌒 **S-CH5 built, dark — the channel train S-CH1..S-CH5 is COMPLETE** (15 seq 48; double env-gated `CHANNEL_DUAL_WRITE` + the new `CHANNEL_RECONCILE_ENABLED`, off ⇒ nothing built): a leader-locked, PERMANENT (never-retiring — 05 §5) sweep that holds CH-INV-1 at drift = 0. Owner-conn census → per workspace `withTenantTx` keyset batches (RLS ENFORCING). **Drift basis (05 §3.4):** email = blind-index/value-byte/domain/status compare; phone = value-byte + status/line_type (the flat phone slot has NO blind index). **Phase rule (the job never guesses):** tenant selection + the batch-boundary abort ride `channels_dual_write` (fail-closed, the S-CH3 mechanism); the composed READ gate then picks the direction per batch — read OFF ⇒ **flat wins** (child primary re-projected from flat; the doc-16 S-CH2 coherence-gap closer — an atomic demote-then-promote swap toward the flat value), read ON ⇒ **child wins** (flat cache re-projected from the child primary). **Degenerate states (05 §Edge):** flat-no-child ⇒ create child from flat (the S-CH3 primitive); child-no-flat ⇒ project flat from child; both-null ⇒ no-op; primary vacuum ⇒ promote + project. Flat-wins BUILT payloads REUSE `planContactChannelBackfill` verbatim (CH-INV-1 single-pathed; creates via `backfillContactChannels`). Skip-loud (undecryptable/incomplete-flat) + phantom no-op (non-representable legacy phone grade — no `updated_at` churn). Metrics `leadwolf_channel_drift_{detected,repaired_flat,repaired_child,skipped}_total` + the `drift_remaining` gauge (> 0 after burn-in = **S2**, runbook §K). Flat-wins/child-wins/degenerate/idempotent/gate-off itests + the pure-decider unit authored; **bun test/typecheck/biome CI-owed** | env `CHANNEL_RECONCILE_ENABLED`/`CHANNEL_RECONCILE_BATCH_SIZE`/`CHANNEL_RECONCILE_BATCHES_PER_TICK`, `packages/core/src/channels/channelReconcile.ts(+.test.ts)`, `contactChannelRepository.{findContactsWithChannelDrift,listWorkspacesWithChannelDrift,countContactsWithChannelDrift,loadLive*RowsForReconcile,demote/promote/writePrimary/projectToFlat primitives}`, `apps/workers/src/queues/channelReconcileSweep.ts`, `apps/workers/src/{register.ts,metrics.ts}`, `packages/db/test/contactChannels.reconcile.itest.ts`; commit `cbccb7c` |
| Contact match-time dup signals + channel-substrate steps (S-C6/S-C7/S-C8) | `04` | 🌒 **S-C6 built, dark** (rides the S-CH4 composed read gate `CHANNEL_READ_FROM_CHILD` + `channels_read`; off ⇒ byte-identical, ZERO marker writes) · **S-C7 SUBSUMED by S-CH4** · **S-C8 SUBSUMED by S-CH5** (drift rows below). S-C6 = the 04 §2 **MATCH-vs-ACT split**: any-value email resolve already shipped by S-CH4's dedup rung; NEW = phone-signal-ONLY + cross-key email-collision → `duplicate_of_contact_id` **SUGGESTIONS** (never upsert/merge/block — a review signal for G21/doc 11), guarded only-when-null, sync `runImport` path only (bulk COPY equiv blocked — drift). T7 phone-path itest + marker-guard + gate-off byte-identity authored; **bun test/typecheck/biome CI-owed** | `packages/db/src/repositories/{contactChannelRepository.ts (findContactIdsByPhoneE164BlindIndexes + PhoneE164BlindIndexHit),contactRepository.ts (markDuplicateSuggestion)}`, `packages/core/src/import/runImport.ts (writeChannelRows signals; call site passes read-gate + matched)`, `packages/db/test/contactChannels.dupsignals.itest.ts` |
| Contact schema evolutions (merge contract, history) | `04` | 🌒 **TRUE-MERGE TRAIN BUILT, dark — S-C1..S-C5 + S-C9 shipped + adversarially verified** (`CONTACT_MERGE_ENABLED` + `contact_merge_enabled` off/off in 0067; engine via `planFieldWrite`, 16-table Class-A re-point, never-double-charge, key-releasing tombstone; customer verb `POST /contacts/:id/merge` + preview; S-C9 staff maker-checker over the SAME engine; S-C2 field-change audit on scalar edits). S-U8 review UI built dark on top. Migrations 0065–0067 journaled (verifier fix `49a20e0`) | commits `a08b377`/`1575504`/`37bc86d`/`6264069`/`e77734a`/`9dbd83b` + `49a20e0`; UI `43774de`/`6378f28` |
| `account_domains` / `account_locations` / `parent_account_id` / accounts soft-delete | `06` | 🌒 **S-A1/S-A3/S-A4/S-A5 DDL landed, inert — dead schema** (15 §M-SEQ seq 53/56/57/58; migration `0061`): `account_domains` (whole-set ws-domain unique → C2-safe + `uniq_account_domains_primary` + `idx_account_domains_account`; source CHECK; `source_import_id` SET NULL) · `account_locations` (type/source CHECKs; primary partial unique; **no** `source_import_id`, per 07 §3) · accounts hierarchy (`parent_account_id` + `root_account_id` [no FK] + `uniq_accounts_ws_id` UNIQUE constraint + composite same-workspace self-FK `SET NULL (parent_account_id)` + self-parent CHECK + `idx_accounts_ws_root`) · accounts `deleted_at` (G18) + online swap of `uniq_accounts_ws_domain` → live-only partial. Contacts-shaped FORCE-RLS + triggers + grants; **no** audit actions and **no** retention seeds (07 §8/15 assign none to the 06 family). Account DTO vocabulary (`accountDomainSchema`/`accountLocationSchema`/`accountHierarchyRefSchema`/`accountOverlayExtensionSchema` + caps) in types. **S-A2 dual-write + S-A1 pass-1/S-A3 HQ backfills NOW BUILT, dark** (`ACCOUNT_DOMAINS_DUAL_WRITE` env + `account_domains_dual_write` flag, seeded off/off in `0062`): `applyAccountDomainWrite` (the 06-family cache-contract single write path) + writer instrumentation + `accountBackfillSweep` (domain pass + best-effort HQ location synthesis via `countryToIso`; WHERE-missing watermark; completeness counts = the S-A6 gate). Remaining: S-A6 read cutover + C2 rung + cycle-guard write path; T-P4 itests + drizzle regen CI-owed | migration `0061` + `0062`; commits `683d0b4` (DDL), `352f7f6` (gate + write path), `647f648` (writers + backfills), `bebe873` (tests — salvaged from the killed agent's in-flight commit 3, imports verified) |
| Outbox-driven import lifecycle (rollups + notify via the transactional outbox; G06) | `09` | 🌒 built, dark (S-Q3/S-Q4 ride `IMPORT_V2_ENABLED` off/off; the fast terminal tx writes `import.rollups`+`import.notify` worker_outbox intents in-tx, ONE leaderless relay drains them, the best-effort completed-handler rollups+notify are retired gate-on; T-Q3/T-Q6 itests CI-owed) | commits `bde8cc8` (S-Q3 rollup producer + merged relay + retire handler rollups), `7e128c7` (S-Q4 idempotent notify + email seam + delivery-lag metric); `packages/core/src/import/runFastImport.ts` (terminal + failed producers), `apps/workers/src/queues/importNotify.ts` (consumer + email seam + metric), `apps/workers/src/register.ts` (merged publishers map + retirements), `packages/db/src/repositories/notificationRepository.ts` (`existsForEntity` dedup), `packages/types/src/importV2.ts` (topics + payloads) |
| Import observability & recovery (reaper · metrics · alerts/runbook · PII hardening) | `09`/`13` | 🌒 built, gate-independent hardening (S-Q5/S-Q7/S-S6; the reaper rides the unified-queue gate; observe/recover only — no happy-path change; T-Q5/T-Q8/T-S6 + Redis-flush drill CI-owed) | `apps/workers/src/queues/importReaperSweep.ts`, `apps/workers/src/metrics.ts` (import counters/gauges), `apps/workers/src/register.ts` (reaper wiring + unified-queue depth specs), `packages/db/src/repositories/importJobRepository.ts` (reaper owner reads), `scripts/lint-import-pii-logs.mjs`, `packages/core/src/import/rejectReason.test.ts`, runbook §K in `worker-platform/13-operational-runbooks.md` |
| Scheduled imports (P5) — per-workspace recurring import definitions + leader-locked fire sweep + CRUD verbs | `08` §9 | 🌒 **P5 built, dark** (dual gate `SCHEDULED_IMPORTS_ENABLED` env + `scheduled_imports_enabled` per-tenant flag, off/off — sweep never constructed env-off; each fire re-checks the tenant flag in-tx, off ⇒ skip): a schedule stores a source object + mapping/strategy/cadence and re-imports it on an interval by creating an ORDINARY fast-lane `import_jobs` row (08 §9 "same trio, same machine" — the engine is never re-implemented). The leader-locked sweep (`leader:scheduled_import_sweep`) fires due schedules idempotency-first: per-schedule tenant tx → getByIdForUpdate → re-check enabled/due → in-tx flag → grant re-eval (live role+policy; loss ⇒ disable+notify) → read/parse/route the stored object (missing/parse/over-fast-pair ⇒ recordFailure, auto-disable at 5) → decideFastAdmission-as-create-status → createJob (idem-key `sched:<id>:<window>` ⇒ double-fire = one job) + advanceAfterFire in the SAME tx → enqueue AFTER commit. v1 = fast pair only (copy-mode scheduled fires deferred — drift). CRUD: POST (multipart template upload + admission/scan/route + cap `MAX_SCHEDULES_PER_WORKSPACE=25`), GET (member+), PATCH/DELETE (creator∪elevated), all gate-on-404. Pure-core unit + repo/idempotency itest authored; **bun test/typecheck/biome + the sweep-in-workers e2e CI-owed** | migration `0063_scheduled_imports.sql` (SHIPPED), `packages/db/src/{schema/scheduledImports.ts,repositories/scheduledImportRepository.ts,rls/scheduledImports.sql}`, `packages/types/src/scheduledImports.ts`, `packages/core/src/import/{scheduledImport.ts(+.test.ts),scheduledImportGate.ts}`, `apps/workers/src/queues/scheduledImportSweep.ts`, `apps/workers/src/register.ts` (sweep wiring, sub-gated), `apps/api/src/features/import/scheduleRoutes.ts`, `packages/db/test/scheduledImports.itest.ts`, env `SCHEDULED_IMPORTS_ENABLED`/`SCHEDULED_IMPORT_SWEEP_EVERY_MS`/`SCHEDULED_IMPORT_MAX_CONSECUTIVE_FAILURES`, runbook §K row |
| Incremental / connected-source / API imports | `08` §9 (connected-source branch) | 🔲 (deferred — the remote-URL/connected-source branch is bound by 13 §8 SSRF forward-guard; v1 ships the stored-object branch only) | — |
| Duplicate-review screen | `11` | 🌒 **built, dark — S-U8 merge review upgrade** (11 §5, upgraded IN PLACE on the shipped Data Health duplicates tab; the dismiss-only "Not a duplicate" verb SURVIVES): each pair gains a **Review** action → a side-by-side per-field merge panel (masked survivor vs loser scalars from the preview endpoint, survivor-suggested = canonical, swap control, pinned-field lock [DM6], child-impact chips, irreversibility copy) → an **IRREVERSIBLE Merge** confirm Dialog → `POST /contacts/:id/merge` with a per-pair `crypto.randomUUID()` Idempotency-Key. Gate = the **CONTACT_MERGE dual gate** (`CONTACT_MERGE_ENABLED` env + `contact_merge_enabled` flag, seeded off/off in 0067): off ⇒ the preview/merge 404 ⇒ the typed `MergeNotEnabledError` hides the Merge affordance across the queue and the surface stays **dismiss-only, byte-identical**. New merge surface is TanStack-shaped (`duplicateKeys`, mirroring `features/import/keys.ts`); the pair LIST stays the shipped `useDuplicatePairs` useState hook (in-place). Company-match tab + queue filters/keyset/match-reason chips DEFERRED (DTO gaps — drift rows). typecheck/next-build/biome CI-owed | apps/web `features/data-health/{mergeApi.ts,mergeDecisions.ts(+.test.ts),keys.ts,hooks/useMergePreview.ts,hooks/useMergeContacts.ts,components/MergeReviewDrawer.tsx,components/DuplicatesSection.tsx,components/DataHealthPage.tsx,hooks/useDuplicatePairs.ts,data-health.module.css}`; renders `packages/types/src/contactMerge.ts` DTOs + `apps/api/src/features/contacts-merge/routes.ts` |

## Gate-state tracker (bulk-pipeline enable gates + rollout flags)

| Gate / flag | State | Notes |
|---|---|---|
| Production S3-compatible object store (`FileStore` adapter) | 🌒 **code built, dark — bucket provisioning ❌ user-owed** (Gate B / G07) | Dependency-free SigV4 adapter `packages/integrations/src/storage/s3FileStore.ts` (multipart streaming PUT, SSE header, presigned expiring GET ≤ 300 s, delete/deletePrefix; still no AWS SDK in repo) selected at BOTH roots (`bulkStore.ts` + worker `register.ts`) via `s3FileStoreFromEnv()` — env unset ⇒ `diskFileStore`, byte-identical. USER-OWED: provision a non-prod + prod bucket and set `BULK_IMPORT_S3_BUCKET/_REGION/_ENDPOINT/_ACCESS_KEY_ID/_SECRET_ACCESS_KEY(/_SSE)`; the Gate-B put→signed-get→expiry itest (`s3FileStore.itest.ts`) runs in CI once the non-prod bucket env lands (skips cleanly until then). FinOps/lifecycle review before GA per 14 R05 |
| COPY-FROM-STDIN spike proven | 🟡 **spike authored, verdict CI-owed** (S-P1/Gate A) | `bulkImport.pipeline.itest.ts` cases 1+5–7 assert all four 12 §3.2 criteria through the real `copyRows` seam (postgres.js 3.4.5 `.writable()`); verdict line in the ADR-0036 addendum stays "pending CI" until the first CI run; red ⇒ 12 §3.3 fallback behind the same seam, never a redesign |
| Real AV/malware scan | 🌒 **code built, dark — scanner deployment ❌ user-owed** (Gate C / G08, S-S2) | `MalwareScannerPort` in core (`security/malwareScanner.ts`, stub = explicit default) + dependency-free clamd INSTREAM adapter (`integrations/security/clamdScanner.ts`), env-selected (`MALWARE_SCANNER=clamav\|stub`) at BOTH wire points: api admission (`malwareScan.ts` → routes.ts + bulkRoutes.ts — scan-before-parse, infected refused pre-job, outage ⇒ 503 `scan_unavailable` FAIL-CLOSED) and the copy drive (`runBulkImport` re-check before staging; infected ⇒ `failed`/`av_infected`; outage ⇒ throw → retry → DLQ). No-new-'skipped' monitor armed with the scanner (reaper gauges `av_skipped_recent`/`av_pending_stale`; §K alert rows). USER-OWED: deploy a clamd sidecar (signature currency = ops, 13 §11) + set `MALWARE_SCANNER=clamav`, `CLAMAV_HOST/CLAMAV_PORT(/CLAMAV_TIMEOUT_MS)`; T-S3's end-to-end EICAR itest then runs in CI. Stub default = shipped behavior byte-identical |
| `BULK_IMPORT_ENABLED` (env kill-switch) | off (default) | explicit-`"true"`-only. **S-I9 graduates this pair** (15 row 40; db-mgmt rule 3 — no new flag): with BOTH halves on AND the IMPORT_V2 pair on, over-threshold imports route to copy on the unified surface (one-shot POST + draft commit) instead of refusing; flipping either half off restores the honest ceiling instantly (§R-P2) — in-flight copy jobs finish, new over-threshold submits 413 |
| `bulk_import_enabled` (per-tenant flag) | seeded off | `global_enabled=false, default=false`. Per-tenant copy-off is the §R-P2 triage lever (AV false-positive storms, R06). **Before any flip: A+B+C green in CI + the ≥100k-row real-file canary E2E + idempotency replay (14 exit) — user/CI-owed** |
| `SCHEDULED_IMPORTS_ENABLED` (env kill-switch, P5) | off (unset; explicit-`"true"`-only) | the P5 sweep is registered ONLY when this is on AND the unified-queue block ran (a scheduled fire has no consumer without the fast lane) — off ⇒ nothing built, the api verbs 404, byte-identical. The instant fleet-wide P5 rollback lever |
| `scheduled_imports_enabled` (per-tenant flag, seeded off in 0063/P5) | seeded off (`global_enabled=false, default=false`) | the second half of the P5 dual gate; each fire re-checks it in-tx (off ⇒ the schedule is skipped, neither disabled nor advanced — a re-flip resumes it) and every api verb 404s while off; flips only after §P5 itests green in CI + a per-tenant canary |
| `BULK_ENRICHMENT_ENABLED` / `ER_SHADOW_ENABLED` (adjacent, not this series) | off / off | context only — distinct flows |
| `JOB_VISIBILITY_SCOPED` (env kill-switch, S-V3) | off (unset; explicit-`"true"`-only) | flag-off = byte-identical (reviewed) |
| `job_visibility_scoped` (per-tenant flag, seeded in S-V1's migration) | seeded off (`global_enabled=false, default=false`) | cohort flips gated on §T-P0 green in CI |
| `IMPORT_V2_ENABLED` (env kill-switch, S-I3) | off (unset; explicit-`"true"`-only) | flag-off = byte-identical legacy import path (T1 is the proof; §R-P1 lever) |
| `import_v2_enabled` (per-tenant flag, seeded in 0054/S-I1) | seeded off (`global_enabled=false, default=false`) | flips only after §T-P1 green in CI + the §R-P1 rehearsal |
| `CHANNEL_DUAL_WRITE` (env kill-switch, S-CH2) | off (unset; explicit-`"true"`-only) | the name doc 05 pins; off ⇒ every email/phone writer byte-identical + zero flag reads (T-CH parity); the instant §R-P3 lever — child rows already written stay inert (unread until S-CH4) |
| `channels_dual_write` (per-tenant flag, seeded in 0059/S-CH2) | seeded off (`global_enabled=false, default=false`) | flips only after §T-P3's T-CH parity + CH-INV-1 itests green in CI; staging first per 05 §Rollout (dual-write on → S-CH3 backfill → drift = 0 → S-CH4). **Also the S-CH3 tenant selector + batch-boundary abort** (re-evaluated in-tx per batch, fail-closed) |
| `CHANNEL_BACKFILL_ENABLED` (env job-level flag, S-CH3) | off (unset; explicit-`"true"`-only) | 05's S-CH3 "job-level flag"; the sweep is registered only when this AND `CHANNEL_DUAL_WRITE` are both on (S-CH3 strictly after S-CH2, 15 §M-SEQ). Off ⇒ nothing is built. Self-terminating + idempotent ⇒ safe to leave scheduled; enable → watch `leadwolf_channel_backfill_remaining` → 0 → re-run post-fleet-dual-write → off. Batch knobs `CHANNEL_BACKFILL_BATCH_SIZE`(1000)/`CHANNEL_BACKFILL_BATCHES_PER_TICK`(10) = 05's per-workspace batch control |
| `CHANNEL_READ_FROM_CHILD` (env kill-switch, S-CH4) | off (unset; explicit-`"true"`-only) | the name doc 05's S-CH4 row pins; the env half of the COMPOSED read gate = this AND the full S-CH2 dual gate AND `channels_read` (read implies dual-write, fail-closed). Off ⇒ every read surface byte-identical to the flat cache + ZERO extra queries (env short-circuit). The instant §R-P3 read rollback: flip off ⇒ reads return to the flat primary cache (still dual-write-maintained); secondaries merely go invisible, nothing lost |
| `channels_read` (per-tenant flag, seeded in 0060/S-CH4) | seeded off (`global_enabled=false, default=false`) | flips only AFTER S-CH3 completeness = 0 (`countContactsMissingChannelProjection`) + drift = 0 + §T-P3 read-cutover itests green in CI; per-tenant canary per 05 §Rollout. Read implies dual-write: effective only when `channels_dual_write` is already on for the tenant |
| `CHANNEL_RECONCILE_ENABLED` (env job-level flag, S-CH5) | off (unset; explicit-`"true"`-only) | 05's S-CH5 permanent-fixture enable; the sweep is registered only when this AND `CHANNEL_DUAL_WRITE` are both on. Off ⇒ nothing built. Unlike the self-terminating S-CH3 backfill it NEVER retires (05 §5) — enable it and leave it; `leadwolf_channel_drift_remaining` at 0 is the steady state, > 0 after burn-in = the S2 alert (runbook §K). Per-tenant SELECTION + the batch abort ride `channels_dual_write`; the composed READ gate (`CHANNEL_READ_FROM_CHILD` + `channels_read`) picks the repair direction (off ⇒ flat wins, on ⇒ child wins). Batch knobs `CHANNEL_RECONCILE_BATCH_SIZE`(1000)/`CHANNEL_RECONCILE_BATCHES_PER_TICK`(10) |

## Drift log

Places where shipped code diverges from this series' design, with disposition
(amend doc vs fix code). Empty at series open.

| Date | Drift | Disposition |
|---|---|---|
| 2026-07-05 | `JobViewer` ships with a required `scoped: boolean` carrying the dual-gate result (doc `10`'s sketch had only `{userId, role}`) — the clean way to honor "required viewer" AND flag-off byte-identity | Amend `10 §4` at next touch; S-V6 deletes the field with the branch |
| 2026-07-05 | The policy-change audit action was never named in `10`/`15` — minted **`import.policy_updated`** (matches `08 §7`'s `import.*` family) | Recorded; `08`/`13` cite the family, no doc edit needed |
| 2026-07-05 | Legacy poll ownership check reads `importedByUserId` off the BullMQ `job.data` payload (doc `10 §5` row said "Redis payload" loosely) | Code shape wins; no doc edit needed |
| 2026-07-05 | `recentBatches` elevated view stays byte-identical (grouped file/source/minute rows have no single creator to attribute) | Per-batch attribution lands with the card split (S-U5, Phase 1) |
| 2026-07-05 | Mapping-template `visibility='private'` list *filtering* deferred with its `08` S-I2 column (S-V4 shipped manage-gates only, per `15` seq-5 wording) | Phase 1 (S-I2) |
| 2026-07-05 | `navConfig` `IMPORTS_DESTINATION` exported but not railed (apps/web has no client-side per-tenant flag reader yet) | Rail entry lands with S-U2; `WIRE(S-U2)` comment marks the seam |
| 2026-07-05 | Enrichment confirm stays `requireRole("owner","admin")` — **stricter** than `10 §5` row 8's creator∪elevated (pre-existing I3 spend-safety posture) | Deliberate; widening deferred, revisit with Phase-1 verbs |
| 2026-07-05 | **Pre-existing, not this program:** types `auditAction` contains `mfa.enroll` but no migration CHECK ever added it — an `mfa.enroll` writer would fail the DB CHECK today | Escalated to the auth/worker-platform track; NOT touched by S-V1's CHECK extension (83 values = current enum minus this drift) |
| 2026-07-05 | **S-I3 transport:** the fast kind rides the unified `bulk-imports` queue with ROWS IN THE PAYLOAD — `09 §1.2`'s "payloads PII-free, always" is deliberately excepted for Phase A (no load-bearing object store until G07; `08 §1.2`/`12 §2.4` are the sanction). Retires at Phase B when the payload slims to `{jobId, scope}` | Documented in `importV2.ts`; no doc edit — 09's rule describes the end state |
| 2026-07-05 | **S-I3 topology:** `08 §1.2`/`09 §1.4` phrase Phase A as "the fast wrapper wraps [the legacy queue's] consumer"; implemented instead per `09 §1.1`'s unified topology (a `fast` kind on `bulk-imports`), which `15 §M-SEQ` seq 12 sequences INSIDE Phase 1 — the legacy `imports` queue/consumer are byte-untouched either way (S-Q8 retires them) | Code shape wins; amend `08 §1.2` wording at next touch |
| 2026-07-05 | **S-I3 ledger:** landed rows (created/matched/duplicate/skipped) get NO per-row `import_job_rows` entries on the fast path — `runImport`'s summary reports them in aggregate only and the engine is mandated UNCHANGED; the rejected-rows ledger IS written per-row. Full landed-row coverage needs an additive engine observer seam | Defer to S-I7 (artifact step); counters carry the truth meanwhile (T4) |
| 2026-07-05 | **S-I3 read model:** gate-on terminal summaries carry counts + histogram with EMPTY `errors`/`rejectedRows` and `addedToList: 0` (the non-PII control row persists neither; legacy Redis summaries carried them inline) | Reject detail arrives with S-I7's artifact pair; `addedToList`'s durable home is an S-I4 DTO decision |
| 2026-07-05 | **S-I3 shed edge:** gate-on, the backpressure 503 can fire AFTER the durable row committed (create → enqueue order is the idempotency-first order) — the row then waits in `queued`, visibly, with no BullMQ job until re-submit (same Idempotency-Key re-submits re-enqueue nothing; a keyless re-submit creates a fresh job) | S-Q5's reaper (Redis-loss re-enqueue from job rows) is the designed healer; acceptable dark |
| 2026-07-05 | **S-I3 side-effects:** fast-path rollups + the `import_complete` notification stay BEST-EFFORT `completed`-handler enqueues, byte-parallel with the legacy handler (`register.ts`) — G06 is NOT closed by S-I3 | By design: S-Q3/S-Q4 move both onto the transactional outbox and retire the two handlers |
| 2026-07-05 | **S-Q1 gating edge:** with the worker now constructed under (BULK_IMPORT ∨ IMPORT_V2), a copy drive/chunk claimed while `BULK_IMPORT_ENABLED` is off FAILS LOUDLY (`CopyKindsDisabledError` → retry → DLQ, operator redrive is idempotent) — under the old both-off construction such jobs would have WAITED for the flag. 09 never specified this sub-case | Deliberate: fail-loud beats silently running a gated pipeline or consuming the job; note for the S-I9 enable runbook |
| 2026-07-05 | **S-Q1 containment:** the dark bulk worker gains the house lock/stall settings (60s/30s/2 → DLQ) + per-kind deadlines via `eventTuning`/`bulkImportKindDeadlineMs`, replacing its bare `concurrency: 1` — a (dark) behavior change sanctioned by 09 Reconciliation #3/#4 | Tripwires extended in `tuning.test.ts` |
| 2026-07-05 | **S-Q2 deferred transport:** `09 §2.2` parks overflow with NO transport (the sweep re-publishes at promotion) — correct for copy (`{jobId, scope}` reconstructable) but a Phase-A fast payload CARRIES THE ROWS, so parking without transport would strand them. Implemented: a deferred fast job also enqueues its payload with a delayed cooperative cap re-check (`IMPORT_DEFER_RECHECK_DELAY_MS`, re-parks at cap with a `:r<n>` id); the leader-locked sweep stays the DB-truth promoter + the copy-drive transport re-publisher, exactly as designed | Phase-A-only mechanics; dies with the rows-in-payload transport at Phase B. `runFastImport`/`register.ts` document it |
| 2026-07-05 | **S-Q2 census:** the per-workspace cap is a SOFT census (no serializing lock — ±1 job under a concurrent-submit race); the atomic fair-share dispatcher is deliberately NOT pre-built (09 §2.1's own rejection of the interim scheduler, worker-platform F7) | By design; `importFairness.ts` header records it |
| 2026-07-05 | **S-Q2 sentinel:** "K=∞ restores enqueue-all" (09 §Rollback) is expressed as `IMPORT_CHUNK_WINDOW=0` (an env int cannot carry ∞); same 0-sentinel for `IMPORT_WORKSPACE_JOB_CAP` (0 = cap disabled) | Recorded; env.ts comments carry the contract |
| 2026-07-05 | **T-Q4 scope:** the S-Q2 itest proves the cap/deferral/promotion MECHANICS (admission, cooperative re-check, oldest-first metered promotion, window helper); the mixed-load contention half of T-Q4 (whale + fast p95 wait) needs the nightly soak rig (12 S-P4 / TP-3) | Split recorded; `importFairness.itest.ts` header cites it |
| 2026-07-05 | **S-Q6 surface:** the shared derivation fn feeds the gate-on poll's `processed` today, but its `percent`/`stage` outputs have NO public field until S-I4's v2 detail DTO (the compatibility-window response keeps the legacy shape byte-for-byte) — 09 §API's "detail response's derived percent/stage" lands then, from this same fn | By design (08 §2.4 window); nothing to amend |
| 2026-07-05 | **S-Q6 cadence vs Phase A:** the copy-side "one delta per ~500-row batch" cadence is a CONTRACT ON THE EXECUTORS pinned as constants — the shipped `bulkProcessChunk` writes its deltas once per CHUNK tx (stricter than the budget, fewer writes) and the fast wrapper once per RUN; neither is loosened. The per-batch write lands when chunk batching itself does | Constants + tests are the tripwire; executors already comply (≤ budget) |
| 2026-07-05 | **S-I7 2nd artifact key:** S-I1 sized ONE `rejected_artifact_key` column (for the single-artifact predecessor); the error-report key rides `import_jobs.options.errorReportKey` (jsonb) rather than an additive column, so the pair ships with no new DDL | Code shape wins; a dedicated column is an optional additive migration if ever wanted |
| 2026-07-05 | **S-I7 vocabulary placement:** the typed reject-code enum is a NEW leaf `packages/types/src/importReject.ts`, NOT importV2.ts as the brief suggested — `rejectedRowSchema` (contacts.ts) references it and importV2.ts imports contacts.ts, so putting it in importV2.ts would cycle; the leaf keeps contacts importing DOWN (the importAdmission.ts leaf precedent) | Code shape wins; no doc edit needed |
| 2026-07-05 | **S-I7 histogram unification deferred:** the ledger `reject_reason` + both artifacts adopt the typed code vocabulary, but the shipped `rejectLabel` / `reject_histogram` KEEP their human-label vocabulary (retyping them ripples into persisted histograms + tests) — 08 §4's "one vocabulary … histogram labels" unification of the histogram is deferred; the code↔label map is 1:1 conceptually | Shipped labels win; align at a later `04`/`08` touch |
| 2026-07-05 | **M6 resolution (S-S4):** 08 §6.2's "signed expiring URLs" is superseded by 13 §4.3's PROXIED-WITH-AUDIT streaming — S-S4 ships proxied delivery (getObjectStream → pinned `text/csv`/nosniff/attachment/no-store headers); presigned URLs are NOT built (documented bounded fallback: TTL ≤ 5 min, audit-at-mint, never in a notification). Resolves 15 §Mismatches ruling M6 | Recorded as M6 required; amend 08 §6.2 / 10 §5 row 5 wording at next touch |
| 2026-07-05 | **S-V5 stricter download bucket:** 10 §7 / 13 §7 mandate a stricter per-user download bucket, but the shipped `rateLimit` middleware exposes no per-route bucket knob — the coarse limiter is applied; the dedicated bucket is config-owed | Deferred; note for the abuse-control follow-up (no knob today) |
| 2026-07-05 | **S-I4 detail shape + `addedToList` home:** `GET /imports/:id` gate-on is an ADDITIVE SUPERSET — the legacy `status`/`progress`/`summary` kept byte-for-byte + v2 `statusV2`/`counts`/`mode`/`percent`/`stage`/`createdBy`/`mergeMode`/`preservePopulated`/`previewSummary`. `addedToList` is OMITTED gate-on (the non-PII control row never persists the list-membership tally; deriving it from counters would lie) — the legacy summary keeps its shape-compat `0`. Durable home decided: it returns with the S-I7 receipt/artifact work, not fabricated here | Honest minimal; `08 §7` detail contract satisfied additively; revisit `addedToList` persistence in S-I7 |
| 2026-07-05 | **S-I4 list cursor:** `GET /imports` uses a COMPOUND `(created_at, id)` base64url keyset matching `idx_import_jobs_ws_created` exactly (row-value `<` predicate), not the single-v7-id cursor `notificationRepository` uses — the composite index is `(workspace_id, created_at DESC, id DESC)`, so id-only would not be index-ordered | Code shape wins; no doc edit |
| 2026-07-05 | **S-I4 cancel scoping + `illegal_state`:** cancel = creator ∪ elevated via the SAME `jobVisibility` predicate as the reads, so its owner-scoping rides `JOB_VISIBILITY_SCOPED` (gate-off ⇒ workspace-wide WITHIN the RLS tenant/workspace wall), consistent with the v2 detail — not an independent gate despite 10 §5 row 4's "strict from birth". 409 `illegal_state` uses a NEW GENERIC `IllegalStateError` in `errors.ts` (not an import-scoped class) | Deliberate (one viewer mechanism, no second flag); the RLS wall is unconditional. Amend `10 §5` note at next touch |
| 2026-07-05 | **S-P2 XLSX ceiling vs fast-path routing ceiling:** the centralized `IMPORT_MAX_XLSX_BYTES` stays **25 MiB** (SHIPPED-CODE-WINS over 12 §5's published 10 MB — lowering it tightens a live admission gate); 12 §5's 10 MB is a SEPARATE fast-path ROUTING byte ceiling `IMPORT_FASTPATH_MAX_BYTES`. admission.ts re-exports the shared constants byte-identically (values unchanged; centralized location only) | Reconcile the two XLSX numbers when S-P4's soak lands (14 §5); documented in `importLimits.ts` |
| 2026-07-05 | **S-I5 refusal slug:** over-threshold routing reuses the 08 §2.3 closed slugs `file_too_large` / `xlsx_too_large` (12 §5's standing choice) via `ImportTooLargeError` carrying RFC-9457 `limit`/`current`/`unit` members — the `import_too_large` slug the brief floated was REJECTED to keep the taxonomy closed; row-vs-byte ceiling is distinguished by `unit`, not a new slug | Taxonomy preserved; no doc edit |
| 2026-07-05 | **S-I6 preserve_populated scope + update_only outcome + template layer:** `preserve_populated` is applied to the SCALAR provenance fields (the pin-protected subset `planFieldWrite` governs), not identity/channel fields (channels follow 05 §6 append-with-dedup, never merge_mode). An `update_only` MISS lands as outcome `skipped` (the typed `no_match_update_only` code exposure rides S-I7's per-row ledger, not the counters). The `request → template → import_policy` precedence's TEMPLATE layer is UNWIRED on `POST /` (no `mapping_template_id` set here) — currently request → policy; the template slots in with S-I2/S-I8 | Byte-identity holds (legacy maps to create_only/create_and_update, preserve=false); template layer is a later step |
| 2026-07-05 | **S-I4 fast-path cancel counter gap:** cancelling a RUNNING fast job keeps committed contacts (stop-remainder) but the terminal guard suppresses the engine's counter/ledger writes, so the cancelled row shows ZERO counters (accounting identity holds trivially, 0=0). `runImport` has no per-row cancel check, so a mid-run fast cancel is best-effort at the terminal/validating boundaries only | Acceptable Phase-A/dark (fast jobs are ≤ threshold); copy mode's per-batch cooperative cancel is the true-remainder path (09 §5) |
| 2026-07-05 | **S-Q5 lease columns absent:** `09 §7 row 2`/`15 seq 26` say the reaper "composes db-mgmt-research/05's lease columns — owned there"; the shipped `0032` trio has **no** `lease_expires_at`/heartbeat columns and 05's reaper step is unbuilt. The reaper therefore recovers from what EXISTS — the durable `import_jobs` control rows + BullMQ job-id liveness (a per-tick snapshot of all live queue ids) — composing the promotion-sweep idiom, not a lease scan | Code shape wins (zero-DDL mandate holds); when 05's lease columns land, chunk-grain lease recovery slots in additively. `importReaperSweep.ts` header records it |
| 2026-07-05 | **S-Q5 no `updated_at` for stall:** `import_jobs` has no `updated_at`/heartbeat column, so the stall detector compares the 7-bucket counter SUM across ticks in-process (09 §8's own prescribed mechanism — "compare counter snapshots per running job"), not a DB timestamp. Snapshots are per-process + pruned to the running set each tick | By design (matches 09 §8); no DDL. A leader change re-seeds snapshots (a running job re-enters its stall clock — at worst one extra window before a stall fires) |
| 2026-07-05 | **S-Q5 fast orphan = honest terminal, not re-enqueue:** rows travel in the BullMQ payload in Phase A (S-I3 drift), so a lost/orphaned FAST job cannot be re-driven from the DB row — the reaper writes the honest `failed` terminal (`markFastImportFailed`, PII-free reason) so no eternal `queued`/`running` row survives. COPY orphans re-publish the drive (staging/object-store is truth). This is the designed healer for drift row "S-I3 shed edge" (the 503-shed visible-queued-row); at Phase B (rows in the object store) fast becomes re-drivable like copy | Phase-A limitation, documented; every job still reaches a terminal state (T-Q5 intent). Re-drivable-fast lands with the object store |
| 2026-07-05 | **S-Q5 artifact re-sweep = flag, not re-run:** `09 §7 row 4` says "re-run OR flagged" — the reaper FLAGS terminal-with-rejects-but-no-artifact jobs via the `import.jobs.artifact_pending` gauge + a log, rather than re-running the writer (which needs a FileStore injected into the reaper + `RejectedRow[]` reconstructed from the ledger — a heavier surface). Operators get the signal today | Deferred (heavier surface, gate-independent value delivered via the flag); re-run wires when the reaper gains a FileStore seam |
| 2026-07-05 | **S-Q5 reaper gating:** the reaper is constructed inside the `BULK_IMPORT_ENABLED ∨ IMPORT_V2_ENABLED` block (it needs the queue handle for liveness + re-drive) rather than literally always-on — but it only heals the non-terminal rows those producers create, so with both gates off there is nothing to reap. `09 §Rollout`'s "ship any time" holds (the metrics-layer additions ARE always compiled/rendered — empty when the reaper never runs) | Deliberate; mirrors the importPromotionSweep placement |
| 2026-07-05 | **S-Q7 no histogram primitive:** `09 §8` names `worker.job.duration` histograms (feeding the fast-lane p95-wait SLO) + per-priority-band `queue.oldest_waiting_age`; the shipped zero-dep metrics layer has counters + gauges only, no histogram type. S-Q7 ships the exit-gate-relevant signals (stall, accounting, artifact-pending, reaper counters, unified-queue depth + DLQ depth) and DEFERS duration histograms + per-band waiting-age to the OTel pass / the soak rig (12 §S-P4 / TP-3) that would actually tune the SLO | Deferred with rationale (matches 09 §8's own "full 10 §4 catalog when the OTel pass lands"); the buildable subset landed |
| 2026-07-05 | **S-Q7 notify-lag reserved not defined:** `leadwolf_import_notify_delivery_lag_seconds` is OWNED by the sibling's S-Q4 (the notify consumer emits it); S-Q7 only reserves the name (a comment in `metrics.ts`/runbook K.1) so the two slices never double-define it | Coordination note; no code owns it here |
| 2026-07-05 | **S-Q7 unified-queue depth exposure:** the `bulk-imports` queue + its DLQ are constructed LOCAL to `startWorkers` (gated), so their handles are lifted to module scope (`bulkImportsQueueHandle`/`bulkImportsDlqHandle`) and pushed into `collectWorkerMetricsText`'s specs only when present — additive; absent while the gate is off renders no row (honest-unknown, no fake zero) | Code shape; enables the 09 §8 DLQ-depth (S2) + queue-depth/age alerts without a new scrape path |
| 2026-07-05 | **S-S6 `reject_reason` already compliant:** `13 §3.3`'s writer rule is ALREADY met by the shipped code — the only `import_job_rows.reject_reason` writer is `runFastImport`'s terminal ledger, which writes `rejectReasonToken(code, column)` (the copy path `bulkProcessChunk` writes no rejects; rejects happen at validate/stage time). S-S6 adds the ENFORCEMENT: T-S6 (`rejectReason.test.ts` — token-shape regex + PII-pattern sweep over the token AND the aggregate error report) + the grep-style log-lint (`scripts/lint-import-pii-logs.mjs`, `bun run lint:import-pii`), not a code change to the writer | Verified compliant; the test + lint are the regression tripwire (13 §11 worst-case #2) |
| 2026-07-05 | **S-S6 ledger `input` encryption DEFERRED (recommended-not-shipped):** `13 §3.2` recommends moving `import_job_rows.input` to `encryptPii` ciphertext "once the artifact writer is the only reader" — that precondition is UNMET (the artifact writer serializes IN-MEMORY `rejectedRows`, not the ledger; the ledger `input` currently has no code reader, but the DESIGNED staff `data:review` drill-down (13 §3.2) + the S-S8 DSAR pointer-scrub both expect to read/scrub the row). Encrypting also RIPPLES on shape: the column is `jsonb` and `encryptPii` returns `Uint8Array` (bytea), so ciphertext-in-jsonb changes the read contract for every future reader + the DSAR overwrite. It is explicitly "recommended not gating". SHIPPED: the writer rule + lint + T-S6; DEFERRED: the `input` encryption, to land WITH the staff-review reader / S-S8 so encrypt-on-write + decrypt-on-read wire together | Deferred with rationale (13 §3.2's own precondition unmet + jsonb↔bytea shape ripple); revisit at S-S8 / the staff drill-down |
| 2026-07-05 | **S-I10 retry sourcing (Phase A):** `08 §6.3` sources the retry child from "the stored source object (Phase B; in Phase A, from the repair artifact)" — but Phase A has NO FileStore in apps/api, so reading the repair artifact is not viable. Implemented: the child sources its rows from the parent's failed+unprocessed `import_job_rows.input` ledger — the DURABLE equivalent of the repair CSV (which `runFastImport` REGENERATES from those same ledger rows), so it is byte-equivalent to "re-import the repair CSV" with no FileStore. A wholly-`failed` fast job writes NO per-row ledger (only rejected rows get `input`), so it yields nothing to retry ⇒ 409 (08 §2.3 "nothing to retry") | Phase-A mechanics; Phase B re-extracts by `row_index` from the stored object. `importJobRepository.listRetryableRows` header records it |
| 2026-07-05 | **S-I10 commit quota mechanics:** reuses the shipped `IMPORT_MAX_COMMITS_PER_HOUR` (S-P2; the constant's own comment says "writer lands S-I10"). Enforced per-workspace-per-hour by counting `import_jobs` created in the last hour (`countJobsCreatedSince`) — a SOFT quota (±1 under a race, mirroring the S-Q2 census). Enforced AFTER the idempotent `createJob` so a keyed REPLAY (returns the same child) never 429s at the cap — the quota throw rolls the fresh insert back. Retry gate = creator ∪ elevated via the SAME `jobVisibility` predicate as cancel (S-I4), not the tighter artifact gate | Deliberate (idempotency-preserving + one viewer mechanism); `errors.ts` `ImportQuotaExceededError` = 429 `import_quota_exceeded` |
| 2026-07-05 | **S-Q3/S-Q4 worker_outbox only — event_outbox SSE stays dark:** `09 §6.2`/`15 seq 24` list an `event_outbox` (SSE) terminal event alongside the `worker_outbox` intents, but `realtimeRelay` is DARK (`REALTIME_SSE_ENABLED` off, S-Q6 reserved the names for "wiring lands a doc-14 slice"). Writing event_outbox rows now would LEAK them as `pending` forever past the un-started relay (climbing `oldestPendingAge`). SHIPPED: the `worker_outbox` half only (`import.rollups` + `import.notify` — the G06 loss-critical must-happen work); the event_outbox SSE producers land with the doc-14 SSE wiring | Shipped reality wins (dark relay); no doc edit — 09 describes the end state, S-Q6/09 §4.4 already gate SSE to doc 14 |
| 2026-07-05 | **S-Q3 merged single relay:** the shipped `outboxRelay` started inside the `BULK_ENRICHMENT_ENABLED` block with only that topic's publisher. S-Q3 refactors to ONE relay over a `outboxPublishers` map accumulated from both the import + enrichment blocks, started once when non-empty — a per-block relay would `markFailed` the OTHER block's topics (leaderless drains share one `worker_outbox`, so a claimed row with no publisher terminally fails). `stopBackgroundRelays` unchanged | Additive; `register.ts` documents the single-relay invariant |
| 2026-07-05 | **S-Q3/S-Q4 commit⇒drive NOT moved (fast + copy):** `09 §6.4`/`15 seq 24` say "drive enqueue → `import.drive` topic". NOT done in this pass: the FAST Phase-A drive carries ROWS in the payload (the S-I3 transport drift) which cannot enter the PII-free outbox (09 §6.4 verbatim: "Fast-mode Phase A keeps its legacy direct enqueue"); the COPY drive (`bulkRoutes`) must write its object to the FileStore BETWEEN create and enqueue, so an in-create-tx intent could publish before the object is stored — it needs a post-store tx (a larger restructure, copy is dark/Phase 2). SHIPPED: the terminal-side producers (the G06 heart — the notification loss §6.1); `IMPORT_DRIVE_TOPIC` is reserved for the Phase-B/copy move | Deferred with rationale; fast moves at Phase B (payload slims to `{jobId, scope}`), copy at a post-store-tx follow-up |
| 2026-07-05 | **S-Q4 notify idempotency + no DDL:** exactly-once effect via `notificationRepository.existsForEntity(type='import_complete', entity=('import_job', jobId))` (insert-if-absent) — NO new unique constraint (09 mandates zero DDL). `entityId=jobId` IS the "jobId+terminal-state" key (a job has exactly one terminal state). The delivery-lag metric (`terminal completedAt → insert`) lives in `importNotify.ts` as a zero-dep module counter (the sibling owns `metrics.ts`/S-Q7) — the snapshot is exported for the S-Q7 renderer to surface. Cancel-terminal notify (the API cancel verb) is deferred (self-initiated, low value; no `import.notify` written there) | Code shape wins (zero-DDL, non-colliding metric); the sibling's S-Q7 drift row reserves the notify-lag metric name |
| 2026-07-05 | **S-U2 nav-rail while dark:** `11 §Rollout` wanted the Imports rail entry gated behind the `IMPORT_V2` dual gate, but `apps/web` has no per-tenant client flag reader (the S-U1 drift). So the entry is railed unconditionally and the history page degrades to an honest "not enabled yet" state (`GET /imports` 404 → `ImportsNotEnabledError`) while the gate is off. The branch is not deployed, so nothing leaks to real users yet | Follows the explicit S-U2 instruction; revisit at the cohort flip if strict-dark nav is wanted (one-line `navConfig` revert). Noted in `navConfig.ts` |
| 2026-07-05 | **S-U2 attribution without a name join:** `importV2.ts §createdBy` says the display-name join "lands with the S-U2 history UI", but S-I4 (shipped) returns only `createdBy.userId`. The UI renders "You" / "Teammate" / "System" from the id alone (via `useSessionIdentity`) — no name. A real name needs a backend users-join on the list DTO | Follow-up (backend list DTO gains a display name); honest minimal attribution ships now |
| 2026-07-05 | **S-U2 elevated "Just mine" is client-side:** the own/all toggle filters the LOADED keyset pages by `createdBy.userId` client-side (so "mine" can under-count until more pages load; "Load more" is hidden in the "mine" view). The full fix is a server `GET /imports?scope=mine` param | Follow-up (server scope param); the members' list is already server-scoped to their own, so this only affects the elevated toggle |
| 2026-07-05 | **S-U2 DataTable not virtualized:** `11 §2/§8.3` mandates a virtualized table for large lists; the shipped `@leadwolf/ui` `DataTable` is not virtualized yet (the S-U1 DS-gap). Keyset "Load more" bounds the DOM per page meanwhile | DS-gap (WIRE): virtualize `DataTable` or swap when a virtualized table ships; keyset paging is the interim bound |
| 2026-07-05 | **S-U3 `useImport` survives (de-give-up'd), not deleted:** `11 §S-U3`/G11 say "delete the `useImport` poller". The poller is shared by the "import into list" DIALOG (which reads its receipt in-place — a modal must not navigate away), so a blanket delete would rework the Lists surface. Shipped: the give-up (attempt ceiling + "taking longer than expected") is deleted, the PAGE flow moves to the durable job page (no `useImport`), and `useImport` stays for the dialog's inline receipt | Give-up deleted (the G11 intent); full `useImport` removal deferred until the dialog also adopts the hand-off model (a Lists-surface follow-up) |
| 2026-07-05 | **S-U3 `[jobId]` route repointed off BulkImportProgress:** the durable job page now owns `/imports/:jobId`; the dark bulk-progress surface (deep-link only, gate-off, no live producer since the S-U1 toggle kill) is unmounted from the route but kept in the codebase | Bulk re-wires at Phase 2 (its own route/entry); `BulkImportProgress` stays exported, dark-parked |
| 2026-07-05 | **S-U3 legacy rejected-download is client-side; v2 job page has no downloads yet:** the job page renders the legacy `summary.rejectedRows` as a client-side CSV (the shipped behavior), but the v2 (gate-on) artifact pair (repair CSV + error report via `GET /imports/:jobId/artifacts/:kind`) is not surfaced here yet | S-U6 adds the artifact-download buttons + the retry-failed dialog to the job page |
| 2026-07-05 | **S-U4 auto-map via dropdowns, not chips:** `11 §3` describes auto-map as "binary mapped/unmapped chips + override"; shipped as PRE-FILLING the existing per-field dropdowns (the dropdown value = mapped, "— not mapped —" = unmapped/don't-import). Same binary semantics, existing DS pattern; no new chip component | Simplification (DS-consistent); a chip affordance is a later polish |
| 2026-07-05 | **S-U4 `update_only` gate-off degrades to `skip`:** the merge-mode triad sends the real `mergeMode`/`preservePopulated` (server wins gate-on) AND a derived `conflictPolicy` for gate-off compat (`create_and_update`→overwrite, else→skip). `update_only` + `preservePopulated` have no legacy equivalent, so gate-off they fall back to `skip`/overwrite respectively — the new capabilities only take effect gate-on | Correct per 08 §5's own compatibility mapping (mirrors core's `conflictPolicyToStrategy`); the strategy is honored fully gate-on |
| 2026-07-05 | **S-P5 numbers fixed in the migration, not the doc:** `12 §6.2` fixes the DIRECTION ("lower scale factors / absolute thresholds", "thresholds sized for burst") but names no values — migration `0056` fixes them: vacuum/analyze scale factors **0.01**, vacuum/analyze thresholds 10k, insert threshold **100k** (at the 100M horizon: vacuum every ~1M changed rows instead of the default ~20M; a 2M burst triggers an insert-vacuum promptly). `fillfactor=90` on `import_jobs` is verbatim | Numbers live in `0056_import_storage_params.sql` (one place); amend `12 §6.2` with them at next touch if wanted |
| 2026-07-05 | **S-P5 two of the four §6.2 tables are unshipped:** `contact_emails`/`contact_phones` (05 S-CH1, subsystem row 🔲) do not exist — `0056` guards their ALTERs with `to_regclass` (order-independent no-ops today). ⚠ **S-CH1's migration must (re)state the same autovacuum parameters** — on any DB migrated before S-CH1, the guarded blocks were skipped and nothing re-runs them | Carried here + in `contacts.ts`'s schema comment so S-CH1 cannot miss it |
| 2026-07-05 | **S-P5 regen note:** 15 seq 34's regen column says "params in schema" — Drizzle cannot express storage parameters (fillfactor/autovacuum), so the schema files carry COMMENTS only (`importJobs.ts`, `contacts.ts`) and the hand-authored `0056` owns the DDL; a `drizzle-kit` regen has no storage-param model to fight (snapshots are hand-stopped at 0028 anyway) | Code shape wins; comment-only is the mechanism |
| 2026-07-05 | **S-P1 addendum placement:** 12 §3.2 says "recorded in an ADR addendum" without naming a file — recorded INLINE in `ADR-0036-bulk-async-job-and-staging-pipeline.md` (the ADR the pipeline stands on), matching the on-disk convention (ADR-0016's inline addendum), not a standalone addendum file. Verdict line is "⏳ pending CI" — this sandbox cannot run the spike; the CI run fills it (never faked) | Convention match; 15 §6.2's flip protocol runs off the CI result |
| 2026-07-05 | **S-P1 criterion-4 watermark scope:** the "re-drive resumes from the byte watermark without re-staging staged rows twice" clause is asserted AT THE REPOSITORY SEAM as COPY atomicity (the aborted band contributes zero rows ⇒ the full-band re-drive lands exactly once); the byte-watermark bookkeeping ABOVE the seam is `runBulkImport`'s resume path (`resumed`/staging-done short-circuit), already exercised by the drive tests — the spike does not duplicate it | Split recorded in the itest header; no doc edit |
| 2026-07-05 | **S-P1 spike knobs:** `SPIKE_THROUGHPUT_ROWS`/`SPIKE_MEMORY_ROWS`/`SPIKE_MIN_ROWS_PER_SEC`/`SPIKE_MAX_RSS_DELTA_MB` env knobs exist for local smoke runs on weak machines; the G09 verdict is valid ONLY at the defaults (100k / 1M / 20 000 / 128) — CI must not set them lower. The "generous CI-variance guard" is a warmup band excluded from timing, NOT a discounted floor | Recorded in the itest header + the ADR addendum |
| 2026-07-05 | **Gate B adapter home:** 05 §6/13 place the prod S3 adapter "at the apps/api composition root" (`s3FileStore.ts` under `features/import/`), but BOTH roots (api + workers) need the identical adapter and apps never import apps — it lives in `@leadwolf/integrations` (the sanctioned vendor-adapter layer both apps already depend on; core stays AWS-SDK-free, honoring the rule's rationale). The roots each call the `s3FileStoreFromEnv()` selection seam | Code shape wins; amend 05 §6 wording at next touch |
| 2026-07-05 | **Gate B FileStore port widened:** `deleteObject` + `deletePrefix` added to core's `FileStore` (S-S7 needs a delete surface; 13 §4.4 job-purge prefix deletion). Both constructors (diskFileStore, s3FileStore) implement them in the same commit; no other implementation exists (repo-verified) | Additive port widening; recorded here |
| 2026-07-05 | **Gate B "presigned multipart upload" refined:** the adapter does server-side SigV4 multipart from the API's own stream (bounded memory, parts with real Content-Length) — no client-facing presigned-part URLs (nothing consumes them; the upload enters through the API's admission envelope S-S1 by design, 13 §1). Client-direct presigned multipart is a future large-file optimization, not a Gate-B criterion regression | Deliberate; 14 gate row's intent (multi-instance shared store, streaming, SSE, expiring GET) is met |
| 2026-07-05 | **Gate B reveal-store rider:** worker `bulkReveal`'s FileStore composition flips with the same env selection (the api's reveal download reads the same backend the worker writes — they cannot diverge); reveal was not named by Gate B but shares `BULK_IMPORT_STORAGE_DIR` today | Same-seam consistency; noted |
| 2026-07-05 | **S-P4 delivery shape — gated itest files, nightly workflow USER-OWED:** the soak scenarios ship as `NIGHTLY_SOAK=true`-gated `*.itest.ts` entrypoints (`packages/db/test/importSoak.nightly.itest.ts` — TP-2/TP-6/TP-4; `apps/workers/test/importSoak.fairness.nightly.itest.ts` — TP-3) so the existing ci.yml glob picks them up and PR runs skip them green (no container started, `beforeAll` bails). NO nightly workflow exists in-repo (only `ci.yml`) — the nightly job (`NIGHTLY_SOAK=true SOAK_ROWS=2000000` / `SOAK_WHALE_ROWS=2000000`, cron) is user-owed CI wiring; parameter sets are documented in the file headers. 15 seq 41's "nightly job in CI" cell = these entrypoints + this owed wiring; the §5 ceiling raise stays gated on the FULL (2M) run green, never a smoke green | Wire a `nightly.yml` (cron, same service containers, the two files + env) — user-owed |
| 2026-07-05 | **TP-4 HTTP half unbuilt:** 12 §8's poll hardening (real `ETag` header, `If-None-Match` → 304, `Cache-Control: private, max-age=2`, per-route limiter engaging first) has NO writer in apps/api — no shipped step owns it yet. The soak's TP-4 probe asserts the FINGERPRINT SEMANTICS the header will carry ((status, counters, completed_chunks) stable between chunk completions, changed after one — the exact ETag basis 12 §8 names) + PK-read poll latency; the HTTP assertions are a `test.todo` until the header ships | The header implementation + HTTP itest land together (small 08/12 follow-up; not in §M-SEQ rows 34/39/41) |
| 2026-07-05 | **TP-3 in-process rig limits:** the fairness soak proves the window bound (whale in-flight ≤ K, exact), two-whale interleave (exact), and the fast-lane ≤ 3-min 12 §Success bar measured commit→terminal CONCURRENT with whale chunking in one Bun process — but the QUEUE-WAIT form of "fast p95 wait ≤ one chunk duration" (a fast job parked behind whale chunks in a real BullMQ worker pool) needs the deployed multi-worker rig, the same split the T-Q4 drift row recorded. Measured chunk duration vs fast p95 is printed for the nightly dashboard comparison | Extends (never duplicates) `importFairness.itest.ts`; the deployed-rig half rides the live-runbook/OTel track |
| 2026-07-05 | **TP-6 number fixed in the test:** 12 names the property (worker RSS independent of file size; the §3.2-criterion-3 128 MB-class discipline "through the full drive") but no full-drive number — the soak fixes `SOAK_DRIVE_MAX_RSS_DELTA_MB` default **256** (loader class × 2 headroom for parse buffers + per-row AES-GCM/HMAC contexts) + plateau growth ≤ 64 MB | Number lives in the test; amend 12 §Testing with it at next touch if wanted |
| 2026-07-05 | **S-P4 scope note:** 15 seq 41's test cell is TP-2/TP-3/TP-4/TP-6 — TP-5 (EXPLAIN plan guard) and TP-7 (limits boundary tests) are OTHER rows' gates (TP-7 landed with S-P2 per the subsystem table; TP-5 is unshipped, owned by the §10 keyset-mandate work) and are deliberately NOT in these soak files | No action; recorded so the soak files are not mistaken for the full 12 §Testing list |
| 2026-07-05 | **S-S2 `import.av_infected` audit row DEFERRED — the 0054 CHECK gap:** ruling M1 assigns `import.draft_reaped`/`import.av_infected` to the P2 audit-CHECK train, and 0054 shipped the P1 list only — a writer today would fail the DB CHECK at runtime, and this slice adds NO DDL. The infected terminal's durable record is `status='failed'` + `av_scan_status='infected'` + `failed_reason='av_infected[:signature]'` + the `av_infected_total` metric/log; the in-tx audit row lands when the P2 CHECK extension rides the next migration train (a one-line DDL TODO marked in `runBulkImport.ts`). NOT routed through an unrelated existing action (taxonomy stays honest) | DDL rides S-I9's P2 train (15 seq 40); code TODO marks the spot |
| 2026-07-05 | **S-S2 quarantine-move DEFERRED (13 §2.2's default):** the FileStore port has no move/copy; deleting outright (the sanctioned fallback) would destroy the false-positive re-scan affordance. Shipped: the infected object STAYS at its key, unreachable (no tenant download path serves source objects; the drive refuses it forever; S-S7's lifecycle/purge bounds it). A `quarantine/` move lands if/when the port gains a copy op | Deliberate; 13 §2.2 named delete-vs-move a config choice |
| 2026-07-05 | **S-S2 fast path has ONE wire point (admission):** 13 §2.2's drive-time re-check presumes a stored object; Phase-A fast rows travel in the payload (S-I3 transport drift) and the original bytes no longer exist at drive time — so fast is scanned at ADMISSION (scan-before-parse, pre-enqueue) and that verdict is the row's verdict. At Phase B (fast files in the shared store) the drive re-check covers fast too, automatically (same runBulkImport gate class) | Phase-A mechanics; matches 14 conflict ⑦ ("the upload-seam scan precedes the store-side one") |
| 2026-07-05 | **S-S2 retry children inherit the parent's `av_scan_status`** (previously hardcoded `'skipped'`): a retry carries no new bytes — its rows are the parent's admitted (and scanned, when configured) ledger rows. The no-new-'skipped' monitor excludes `source_file LIKE 'retry:%'` rows for the same reason (a retry of a pre-scanner parent is honest history, not a scan bypass). Byte-identical today (parents are all 'skipped') | Code shape wins; recorded here |
| 2026-07-05 | **S-S2 copy-drive outage terminal:** a REAL scanner erroring at the drive throws into the retry budget → DLQ (fail-closed; staging never starts) — the job row stays `validating` rather than terminalizing `av_unavailable` on exhaustion (13 §2.2's terminal needs a failed-hook write for copy kinds, which only dead-letter today). Operator redrive after scanner recovery is idempotent; the stall/DLQ alerts surface it (§K) | Deferred refinement: an `av_unavailable` terminal write can ride the copy failed-hook when copy-mode engagement (S-I9) lands |
| 2026-07-05 | **S-S2 tenant notify on drive-infected deferred:** the copy path writes no `import.notify` outbox intent anywhere yet (the S-Q3/S-Q4 "commit⇒drive NOT moved" drift) — the infected terminal notifies OPERATORS (metric + log, §K row); the creator's neutral "failed a security scan" notification rides the copy-notify follow-up | Consistent with the existing copy-notify posture; 13 §2.2's notification lands there |
| 2026-07-05 | **S-S2 monitor is gauge-shaped, not event-shaped:** 13 §2.3's "monitor alerts on any new skipped row" ships as reaper-tick owner-connection counts (`av_skipped_recent` 24 h look-back / `av_pending_stale` 1 h SLA), published only while `MALWARE_SCANNER≠stub` — an alert on >0 is equivalent and self-clearing, with no new event plumbing | Code shape wins; thresholds in §K K.1 |
| 2026-07-05 | **S-S7 `import_artifacts` class seed rides the next migration train:** the class is registered in the types vocabulary (`retentionDataClass`) but deliberately NOT added to `DEFAULT_RETENTION_POLICIES` — the 0025 seed rows "MUST match DEFAULT_RETENTION_POLICIES exactly" (schema/retention.ts invariant + the retention itest's 12-row assertion), so the policy row + the DEFAULT entry move TOGETHER in the next migration train (no DDL in this slice). Enforcement never waited on the seed: the sweep is env-driven (`IMPORT_ARTIFACT_TTL_DAYS`), exactly 13 §4.4's "recorded as documentation even though enforcement is lifecycle config" | Seed rides the P2 train with the audit-CHECK extension |
| 2026-07-05 | **S-S7 lifecycle is OUR sweep, not bucket-native lifecycle rules:** 13 §4.4 says "FileStore lifecycle TTL"; shipped as the leader-locked worker sweep (delete objects → null keys) because bucket-native rules can't null the DB keys (the honest-"expired" half) and must work identically on disk/R2/MinIO/S3. A provider-side lifecycle rule on `imports/` MAY be added as belt-and-suspenders when the bucket lands (Gate B FinOps review) — it must be ≥ the sweep TTL so the sweep always nulls keys first | Code shape wins; note for the bucket-provisioning runbook |
| 2026-07-05 | **S-S7 key-less legacy artifacts ride the job purge, not the TTL:** copy jobs' legacy `rejected-rows.csv` has NO DB key column (the phase-6 deferred link), so a job with ONLY that artifact never enumerates as a TTL candidate — its objects go with the job's hard-purge (`purgeImportJobObjects` prefix delete). Keyed (fast S-I7) jobs DO delete the legacy key opportunistically on expiry (idempotent no-op when absent). Copy-mode artifact generation itself is the dormant S-I7 hook (existing drift row) — by the time copy artifacts exist they carry keys | Bounded gap, closes with the copy artifact hook |
| 2026-07-05 | **S-S7 job hard-purge half is a SEAM, not a live deleter:** 13 §4.4's "the retention deleter for import jobs deletes the job's FileStore prefix before the row delete" — no import-jobs row-purger exists anywhere yet (the retention engine's import classes are shadow; S-S8 unbuilt). Shipped: core's `purgeImportJobObjects(fileStore, jobId)` + the ordering contract in its header, so the future purger composes it instead of re-deriving | The purger lands with the retention-enforce flip / S-S8 |
| 2026-07-06 | **S-I8 draft source key = the bulk idiom, not `drafts/<jobId>/`:** the draft's object lands at `imports/<uploadId>/source.<ext>` (a fresh uuid, the exact shipped `bulkRoutes.ts` key shape) because `source_file` is NOT NULL at insert and `createJob` assigns the jobId from the DB default — a jobId-keyed path would need a post-insert key rewrite the repo deliberately doesn't expose. The reaper/cancel delete by the EXACT key off the row, so the shape is inert; the known job-purge-prefix gap (`purgeImportJobObjects` misses uuid-keyed sources) is the SAME pre-existing bulk gap, unwidened | Code shape wins (mirrors bulk); the prefix gap is the existing S-S7 note |
| 2026-07-06 | **S-I8 mapping stays HEADER-keyed; ordinal addressing deferred:** 08 §3.2's "stored mapping addresses columns by ordinal index" is NOT implemented — the shipped `columnMappingSchema`, `mapRow`, both engines, templates, and the wizard are all header-keyed, and re-keying is a cross-cutting contract change. Consequence honored instead of hidden: duplicate headers stay unmappable to distinct fields, so the auto-map REFUSES to map any duplicated normalized key (both columns stay unmapped until the user picks — the 08 §Edge cases behavior) | Deferred to a dedicated mapping-addressing slice; `headerAliases.ts` records it |
| 2026-07-06 | **S-I8 auto-map = alias table + canonical names only:** 08 §3.2's rungs (b) workspace custom-field keys, (c) 05 §6 channel slots, and the sampled-value type-inference tiebreaker are deferred — (b) needs a DB read in a pure module (and the wizard's "+ Create field" flow is the EXISTING `POST /api/v1/custom-fields` registry endpoint, cross-linked not duplicated), (c)'s child tables are 🔲. The seeded vendor-header corpus covers the common HubSpot/Salesforce/ZoomInfo/Apollo export headers | Rungs land with their substrates (registry read; S-CH1); binary + override parity holds |
| 2026-07-06 | **S-I8 fast-pair refusal at DRAFT UPLOAD (byte half pre-buffer):** the draft create refuses over-threshold files at upload (`file_too_large`/`xlsx_too_large` — bytes BEFORE the buffer/parse, rows post-parse), sanctioned by 08 §1's "an over-threshold file is refused at upload" for the pre-copy window. PREVIEW and COMMIT re-assert the same pair (CSV byte gate off the row's `file_size` BEFORE the object is read + the routing row ceiling post-parse), so no draft verb ever scans/buffers past what commit admits — the preview-scan bound 08 §4/12 §5 requires. When G07+G09 clear, upload/preview lift and only commit's routing decides | The lift is S-I9's copy-engagement slice; constants are S-P2's |
| 2026-07-06 | **S-I8 commit re-parses on the REQUEST thread (Phase-A bound):** commit reads the stored object, re-parses, and enqueues with ROWS IN THE PAYLOAD — the S-I3 transport drift extended to the commit verb (08 §1.2 Phase A: no `{jobId, scope}` slim payload until G07 makes the store load-bearing multi-instance). Memory is capped by the same fast pair (≤ 10 MiB CSV / ≤ 25 MiB XLSX admission, ≤ threshold rows); the parse runs OUTSIDE the row lock (the object is immutable post-upload; the locked phase re-checks state) | Dies at Phase B with the rows-in-payload transport |
| 2026-07-06 | **S-I8 commit idempotency = `options.commitIdempotencyKey` (no DDL):** 08 §2.3's "key required; replay returns the first response" ships as a REQUIRED `Idempotency-Key` header recorded in the jsonb options on the commit transition — a replay against a non-draft row with the matching key answers the same 202 (BullMQ's stable `import-fast:<jobId>` id dedupes transport regardless); a NON-matching key answers 409 `illegal_state`. No new column | Code shape wins; a dedicated column is optional additive DDL if ever wanted |
| 2026-07-06 | **S-I8 commit-quota census excludes drafts:** `countJobsCreatedSince` now filters `status NOT IN ('draft','uploading')` — an uncommitted draft is not a commit (08 §2.3: the quota is the COMMIT quota; upload rides the rate bucket + admission caps). Two accepted soft-quota skews, same ±1 posture as S-I10: a committed draft's `created_at` pre-dates its commit (under-count for old drafts), and commit enforces `recent + 1 > cap` (counting itself) where retry counted its own fresh insert | Soft quota by design; an exact commit-time census needs a committed-at column (not worth DDL) |
| 2026-07-06 | **S-I8 `import.draft_reaped` audit DEFERRED — the 0054 CHECK gap (ruling M1, the S-S2 precedent):** the action is NOT in the audit-action CHECK (M1 assigns it to the P2 train) and this slice adds no DDL — a writer today would fail the DB CHECK at runtime. The reap's operational record = the structured log + `draft_reaped_total` counter + `draft_reap_candidates` gauge; the in-tx audit write lands with S-I9's P2 CHECK extension (DDL TODO marked at the reap site). Reap ordering is ROW-FIRST (draft-pinned DELETE; a raced commit wins and nothing is touched) THEN object — the opposite of the artifact sweep's objects-first, because here the row's deletion is what makes the object unreferenced; the crash window leaves a bounded logged orphan object, never a dangling row | DDL rides S-I9's P2 train (15 seq 40); code TODO marks the spot |
| 2026-07-06 | **S-I8 cancel-from-draft keeps the auditable row:** cancel ≡ discard (08 §2.2) deletes the stored object best-effort AFTER the cancel tx commits (a store hiccup must never roll back the transition) but KEEPS the row as `cancelled` — 08 §2.1's "row deleted" applies to the REAPED exit only, and `import.cancelled` needs a row to point at. Consequence: a cancelled ex-draft appears in history as `cancelled` (the "never listed" promise covers `draft`-status rows, which the list now excludes by default with the `?state=draft` wizard-resume opt-in — vacuously byte-identical pre-S-I8 since no drafts existed). A failed object delete leaves a bounded orphan (cleaned by the purge horizon) | Deliberate; amend 08 §2.1 wording at next touch if the distinction reads ambiguous |
| 2026-07-06 | **S-I8 template layer contributes MAPPING only:** `PUT /imports/:id/mapping` accepts a `templateId` (applies its stored mapping when `mapping` is absent; records `mapping_template_id` provenance, full-replace = a template-less PUT clears it), but the 08 §3.1 template STRATEGY block is S-I2's unshipped column — strategy resolves request → workspace `import_policy` default (the S-I6 precedence minus its middle layer). Preview histogram keys are the TYPED reject codes, not the terminal `reject_histogram`'s human labels (the standing S-I7 two-vocabulary drift, applied consistently) | Template strategy slots in with S-I2; the draft ref/mapping surfaces need no change then |
| 2026-07-06 | **S-U7 gate probe = the drafts list, not a blind POST:** gate-off, `POST /imports` without `mapping` answers the LEGACY 422 ("mapping must be…"), not a 404 — only the per-draft verbs 404 — and a blind draft-create probe would upload the whole file once per pick gate-off. Shipped: the wizard probes `GET /imports?state=draft` (404 ⇒ `ImportsNotEnabledError` ⇒ today's client-side flow, silently; result cached 60 s, no retry). Gate-off cost: one 404'd GET per file pick (same posture as the history page's list read); UI byte-identical. A gate-on draft-create REFUSAL (`file_too_large`/infected/empty) surfaces its message AND falls back to the client-side card, so the user is never blocked — the one-shot submit re-refuses with the same honest message | Code shape wins; the mission's "404/gate-off ⇒ fall back silently" is honored via the probe |
| 2026-07-06 | **S-U7 resume restores preview/confirm, NOT the mapping grid:** headers + the saved `column_mapping` ride ONLY the create-time draft ref (`ImportDraftRef`) — no read DTO exposes them (`GET /imports/:id` = `ImportJobDetailV2`: strategy + cached `preview_summary`; the drafts list item has neither). So `?draft` resume re-enters at preview/confirm — the SERVER holds the saved mapping, so preview re-runs and commit work without the client ever knowing it — restoring: filename, mergeMode/preservePopulated (display), the cached projection counts (samples recomputed on demand via re-run). NOT restorable: the header list + grid editing; `?step=map` coerces to preview with honest discard-and-reupload copy. A draft resumed before any mapping was PUT answers preview's honest 422 with a Discard affordance | Honest minimal per the shipped DTOs; backend follow-up: additive `headers`/`columnMapping` members on the gate-on draft detail would make resume fully grid-editable |
| 2026-07-06 | **S-U7 draft path engages in the hand-off flow only** (`onStarted` present — the `/imports/new` page): the "import into list" dialog keeps the one-shot inline flow even gate-on (its receipt reads in place; the draft flow's commit hands off to the durable job page, which a modal must not navigate to). Steps (Map → Preview → Confirm) render ONLY while a server draft backs the wizard — gate-off keeps the single-card one-shot layout byte-identical (the canary rule), so `?step`/`?draft` are inert gate-off | Deliberate; the dialog adopts the draft flow when it adopts the hand-off model (the standing S-U3 `useImport` follow-up) |
| 2026-07-06 | **S-U7 mapping PUT on step-advance, not debounced per-edit:** one `PUT mapping` + one `POST preview` per "Continue to validation" — 11 §3 leaves the save cadence open; on-advance is chosen for simplicity and keeps the preview trivially fresh (editing the mapping means re-advancing = re-PUT + re-preview, the shipped `invalidatePreview` discipline transposed). Template provenance rides the PUT (`templateId`) only while an applied template is still unedited | Recorded; a debounced per-edit autosave is a later polish if mapping sessions grow long |
| 2026-07-06 | **S-U7 URL sync = `history.replaceState` (the BillingPage pattern), not `router.replace`:** same replace semantics (back/refresh land on the right step; no history spam), but no `useSearchParams`, so no Suspense/prerender (CSR-bailout) constraint on the `/imports/new` route — this sandbox cannot run `next build`, so the shape with zero build-time coupling is chosen deliberately. Deep-link params are read off `window.location` on mount | Code shape wins; swap to `router.replace` + Suspense boundary any time if router-state coupling is ever needed |
| 2026-07-06 | **RESOLVED — S-S2 `import.av_infected` audit deferral (2026-07-05 row above):** migration `0057_import_p2_audit_actions.sql` re-issues the audit-action CHECK with exactly ruling M1's P2 pair (`import.av_infected`, `import.draft_reaped`; `mfa.enroll` drift stays excluded, still the auth track's), mirrored in types `auditAction` + schema `billing.ts`. The infected terminal now writes `import.av_infected` IN-TX with the `failed` transition (`runBulkImport.ts` failInfected — actor null = system; facets non-PII: job id + signature LABEL only); the DDL-TODO comment is gone | Closed by S-I9's P2 train (15 seq 40); Drizzle snapshot regen rides CI |
| 2026-07-06 | **RESOLVED — S-I8 `import.draft_reaped` audit deferral (2026-07-06 row above):** with 0057 in the CHECK, the reaper writes `import.draft_reaped` in the SAME draft-pinned tx as the row DELETE (`importReaperSweep.ts` job 4 — a reap that can't record itself doesn't happen; a raced commit still wins and writes nothing). Facets non-PII: job id + `ageHours` (census now carries `created_at`). Log + `draft_reaped_total` counter stay as the operational record; row-first-then-object ordering unchanged | Closed by S-I9's P2 train (15 seq 40) |
| 2026-07-06 | **S-I9 draft upload/preview lift is ROW-HALF ONLY:** the 2026-07-06 S-I8 row promised "when G07+G09 clear, upload/preview lift"; shipped: engaged, an over-ROW draft is admitted at upload and previews (the full pass stays bounded by the UNCONDITIONAL ≤ 10 MiB CSV byte cap), and commit's routing converts it to copy — but an over-BYTE CSV is still refused at draft upload, because every draft verb BUFFERS the object (preview's full pass included) and 08 §4's constant-memory streaming preview is unbuilt. Over-byte files enter copy via the one-shot POST (byte-half short-circuit — no request-thread parse) or `/imports/bulk`, which never re-buffer for preview | Byte-half lift lands with a streaming preview slice; recorded so the wizard's ceiling copy stays honest |
| 2026-07-06 | **S-I9 one-shot copy writes NO `import.committed` audit row:** the one-shot fast path writes none either (08 §2.3 calls the one-shot "upload+commit combined"; §7's audit covers the actor-initiated VERBS — the draft COMMIT verb audits in both modes, `mode:'copy'` + admission in the facets). Kept symmetric rather than minting an audit row only for the copy branch of the same 202 | Deliberate parity; widen BOTH one-shot modes together if compliance ever wants submit-time audit |
| 2026-07-06 | **S-I9 `/imports/bulk` rows now stamped `processing_mode='copy'` + `source_filename`** (previously NULL — the S-I1 columns predate the bulk route's writers): the delegate marks the trio identically to the unified surface. Side effect, an IMPROVEMENT: the S-Q5 reaper's mode-split treated a NULL-mode bulk row as fast ⇒ orphan-FAIL; a marked row is copy ⇒ RE-DRIVEN from the stored object (the designed recovery). Legacy bulk engine behavior untouched (still reads `conflictPolicy`; strategy columns keep their defaults) | Additive, dark; no doc edit — 08 §1 always defined copy rows as marked |
| 2026-07-06 | **S-I9 copy commit skips S-Q2 `deferred` admission:** a copy-verdict commit transitions draft → `queued` directly — S-Q2's cap/census is the FAST lane's backpressure; the copy lane's is the drive queue's shed (typed 503 inside the injected producer, AFTER the row committed ⇒ the row waits honestly `queued`) + the reaper's `copy_redrive` healer (the shipped bulk posture; 09 §2's fairness scheduler owns copy admission when S-Q9-era work lands) | Code shape wins; matches `/imports/bulk`'s shipped posture exactly |
| 2026-07-06 | **S-I9 deprecation signal = RFC 9745 `Deprecation: true` + `Link: </api/v1/imports>; rel="successor-version"`** on `/imports/bulk` responses (08 §1.2 names the window but no header; a standard machine-readable signal chosen over an ad-hoc one). Set AFTER the env kill-switch middleware, so the dark surface's responses are byte-identical | Minted here; amend 08 §1.2 at next touch if a `Sunset` date is ever set |
| 2026-07-06 | **RESOLVED — S-P5 autovacuum tripwire (2026-07-05 row above):** migration `0058_contact_channels.sql` (S-CH1) re-states 0056's exact parameters UNGUARDED on both now-existing tables (`contact_emails`/`contact_phones`: vacuum/analyze scale factors 0.01, thresholds 10k, insert threshold 100k) — on any DB migrated before S-CH1, where 0056's to_regclass-guarded blocks were no-ops, 0058 applies them at table birth. `contacts.ts`'s carrier comment updated to record the closure | Closed by S-CH1's train (15 seq 44) |
| 2026-07-06 | **S-CH1 audit-CHECK packaging — 07 §8 vs 15 seq 44:** 07 §8's S-C2 row still bundles 05 §7's four `channel_*` actions into S-C2's "one CHECK migration at PR time" with `contact.merge`, but ruling M1 (15 §Mismatches) rules the CHECK is extended once per phase, in that phase's train, before its first writer — S-CH2 writes `channel_*` in Phase 3, so `channel_added`/`channel_promoted`/`channel_deleted`/`channel_primary_demoted` land in 0058 (S-CH1's train), mirrored in types `auditAction` + schema `billing.ts`; `contact.merge` stays P4/S-C2's. Followed 15 (the sequencer; the 0053/0054/0057 per-phase precedent) | 15 governs (M1); amend 07 §8's S-C2 row wording at next touch |
| 2026-07-06 | **S-CH1 `phoneLineType` widened in place (05 §1.5's mandate):** the shipped 4-value zod enum (`mobile\|landline\|voip\|unknown`) is now the 14-value union taxonomy — DM1: ONE vocabulary, `types/contacts.ts` stays the single symbol of record shared by the flat `contacts.phone_line_type` cache and `contact_phones.line_type`'s DB CHECK. Purely additive: every shipped writer (Twilio verifier maps fixedVoip/nonFixedVoip → `voip`) emits only the legacy four; consumers type it pass-through (`string \| null` at the UI edge), no exhaustive switches found. The flat column's varchar(20) fits the longest new value exactly (`fixed_line_or_mobile` = 20 chars); the child column is varchar(24) | Sanctioned by 05 §1.5 verbatim ("widened in place"); recorded so the enum growth is never mistaken for drift |
| 2026-07-06 | **S-CH1 retention seed invariant moved to 14 rows:** `DEFAULT_RETENTION_POLICIES` gains `contact_emails`/`contact_phones` (ttl null, shadow — mirror `contacts`, 05 §7) and 0058 seeds the matching `retention_class_policies` rows; the retention itest's exact-count assertion updated 12 → 14. `import_artifacts` deliberately did NOT ride this train — its DEFAULT entry + seed stay S-S7-owed (the 2026-07-05 S-S7 row stands; a P2 lifecycle-class decision, not P3's) | Invariant (seeds ≡ DEFAULT) holds at 14; S-S7's seed still owed |
| 2026-07-06 | **S-CH1 rls-file ordering:** `rls/contactChannels.sql` sorts BEFORE `rls/contacts.sql` in applyMigrations' sorted glob, so it cannot rely on contacts.sql's `set_updated_at` definition (every prior reuser sorts after) — it defines the function itself via a byte-identical `CREATE OR REPLACE`, idempotent in either order | Code shape; noted for the next early-sorting rls file |
| 2026-07-06 | **S-CH1 05↔07 consistency re-check:** the full 07 §8/§3/§4 S-CH1 inventory (2 tables, 4 uniques, 3 signal/fetch/facet indexes, FK on-deletes, RLS shape, seed rows) matches 05 §1–§2 exactly — no divergence beyond the M1 packaging row above; 07 §2.1's ER omits some 05 §1.1 columns by its own "load-bearing columns only" legend (not drift). The 05 §2.2 email/phone unique asymmetry is the spec of record (07 §1 axis 3's verdict) and is what 0058 lands | Verified at implementation; nothing to amend |
| 2026-07-06 | **S-CH2 per-tenant flag MINTED — `channels_dual_write`:** 05 pins only the env half (`CHANNEL_DUAL_WRITE`, used verbatim) and reserves the per-tenant `feature_flags` dual-gate for S-CH4 canarying "if wanted" — the phase brief mandates the dual gate at S-CH2 already (the house rollout posture, 01 §7.3), so the per-tenant key is minted per the 0054 seed precedent. The key deliberately follows the brief's `channels_dual_write` (no `_enabled` suffix, unlike `import_v2_enabled`/`bulk_import_enabled`) | Recorded; amend 05 §Rollout at next touch to name both halves |
| 2026-07-06 | **S-CH2 worker-side gate evaluation:** 05 §5 never says WHERE the gate is read. Shipped: no job-payload carry — the import engine evaluates ONCE per run in its own scoped tx (fail-closed on error ⇒ flat-only; a 10k-row run must not re-read per row), enrich/reveal evaluate IN-TX per op (two PK lookups sharing the caller's tx fate). Identical mechanics in apps/api and apps/workers (both reach writers through withTenantTx); env-off short-circuits to zero queries. A mid-run per-tenant flip takes effect on the next run/op, not mid-import | Code shape wins; note for the 05 §Rollout amendment |
| 2026-07-06 | **S-CH2 `applyChannelWrite` home = packages/db, not packages/core:** 05 §3.1's "working name … in `packages/core`" is superseded by the phase brief — the single write path lives at the repository layer (`contactChannelRepository`), taking BYTES only; DM1's crypto/normalizers stay in core (`channels/channelDualWrite.ts` builds the byte payloads). Keeps db free of crypto and the SQL in the one data-access layer (house rule: repositories are the only data access) | Brief governs; amend 05 §3.1's location note at next touch |
| 2026-07-06 | **S-CH2 dual-write-era CH-INV-1 gap on overwrite-of-a-different-value:** 05 §3.3/§6 mandate import/enrichment NEVER flip an existing child primary (implemented), but the SHIPPED flat writers still overwrite `email_enc`/`phone_enc` unconditionally on an updating match — so an overwrite import (or provider fill) that changes a contact's channel VALUE leaves flat holding the new value while the child primary keeps the old one (the new value lands as a SECONDARY). This is the 05 §3.4 phase rule working as designed: flat is authoritative, the S-CH5 sweep's flat-wins repair direction closes it (and S-CH3's re-run covers the backfill tail). Asserted honestly in the parity itest (the enrich-second-phone case) | **CLOSED by S-CH5 (`cbccb7c`):** the reconcile sweep's flat-wins direction repairs exactly this — an atomic demote-then-promote swap makes the flat-valued row (the appended secondary) the sole primary, byte-exact to flat, restoring CH-INV-1. Itest-proven (the coherence-gap swap case). Until S-CH5 is enabled the gap is by-design (flat is authoritative; reads stay flat); at S-CH4 writers go child-first and the drift stops arising, and S-CH5 keeps it at 0 in both eras |
| 2026-07-06 | **S-CH2 primary byte-refresh vs 05 §Edge "first seen wins `value_enc`":** a dedup HIT on the PRIMARY row refreshes its `value_enc` (and upgrades e164 material when newly parseable) to the op's bytes — because every re-import RE-ENCRYPTS the same value (random-IV AES-GCM) and the primary row must stay a byte-exact projection of the flat cache (CH-INV-1). "First seen wins the storage form" is honored for SECONDARIES (flat never holds them) and becomes the general rule at S-CH4 | Phase-scoped refinement; recorded so the S-CH4 cutover restores 05 §Edge verbatim |
| 2026-07-06 | **S-CH2 enrichment email index-form divergence (pre-existing, mirrored not fixed):** shipped `enrichContact.toWriteValues` derives the flat `email_blind_index` from the trim+lowercase form WITHOUT the plus-tag strip (`normalizeEmailForIndex`) the import path uses — a pre-existing DM1 divergence. The child row mirrors the FLAT bytes verbatim (CH-INV-1's byte equality wins over re-deriving), so a plus-tagged provider email would key differently than the same email import-born. Not fixed here: changing the flat derivation is a live-behavior change outside S-CH2's gate | Escalate to a small dedicated fix (align toWriteValues on `normalizeEmailForIndex`) — flag-independent, but it touches live dedup keys so it needs its own migration thought |
| 2026-07-06 | **S-CH2 `extension` stays NULL:** 05 §4 populates `extension` "from the parser's ext output or an explicit import column" — the shipped `toE164` returns only the E.164 string (no `ext` exposure; DM1 forbids a second parse path here), and no import mapping slot for extensions exists (05 §6's channel slots are the S-CH4-era wizard work). The column waits for both | Lands with the 08 channel-mapping slots / a `toE164` widening that returns the parsed ext |
| 2026-07-06 | **S-CH2 scope — NOT instrumented:** (a) the dark bulk COPY chunk merge (`bulkProcessChunk`) — its channel material must ride staging per 05 §6's `channels` jsonb extension of `prepareContact` (ciphertext computed BEFORE COPY; the phone plaintext is gone by merge time), a build of its own; the COPY pipeline is double-dark (`BULK_IMPORT_ENABLED` off) so no gate-on write path is missed in prod. (b) `editContactFields` — `contactFieldEditSchema` is scalar-only; NO shipped user surface writes email/phone values, so there is nothing to instrument until doc 04's channel API (S-CH4 era) ships the add/edit/promote verbs. (c) No `channel_*` audit rows are written yet — 05 §3.3/§7's audited ops are the user-facing verbs (make-primary, verification demotion) which don't exist; the import bulk path never audits per row (house rule). The 0058 CHECK actions stay written-by-nobody | (a) rides the 05 §6 staging extension with S-CH4's ladder work; (b)/(c) ride doc 04's channel API |
| 2026-07-06 | **S-CH2 cap/collision surfaced as logs only:** the import writer logs non-PII `capped`/`collision` outcomes (contact id + outcome) — the 08 §4 per-row WARNING band (`channel_capped`, and the A↔B duplicate SIGNAL for the G21 review queue) has no substrate yet (S-C6/the artifact warning contract). 05's "append up to the cap and warn" is honored operationally, not yet user-visibly. The cross-contact phone E.164 signal is likewise unwritten (S-C6's rung) | Warning-band + duplicate-signal wiring ride S-C6 (15 seq 50) |
| 2026-07-06 | **S-CH3 watermark = the WHERE-missing selection, not a persisted cursor:** 15 §2.1's letter says "progress watermark = `(workspace_id, last contact id)` persisted per batch commit"; shipped with NO stored watermark and NO DDL — the selection predicate (flat key present AND no live child row of that channel) makes every pass re-runnable and a crash/abort resume by re-selection, which is strictly stronger than a stored cursor (a cursor can go stale against concurrent S-CH2 writes; the predicate cannot). The in-run keyset cursor over `contacts.id` still exists (15's 1k-batch walk), it just isn't persisted. The same predicate is shared verbatim by the batch selection, the owner census, and the S-CH4 completeness count, so the gate can never disagree with the walker | 15 §2.1's *purpose* (resume + idempotency, proven by the interrupt/rerun itest) is met with less machinery and zero DDL (the phase brief's "no DDL unless 15 demands it"); amend 15 §2.1's watermark sentence at next touch |
| 2026-07-06 | **S-CH3 tenant selection + abort = the S-CH2 dual gate, re-evaluated per batch:** 15/05 name a "job-level flag + per-workspace batch control" and a "kill flag checked at every batch boundary" but never say WHICH tenants backfill. Shipped fail-closed: the sweep registers only under `CHANNEL_DUAL_WRITE` + the new `CHANNEL_BACKFILL_ENABLED` env pair; the owner census is fleet-wide, but every batch tx begins by evaluating `isChannelDualWriteEnabled` (env + per-tenant `channels_dual_write`) — flag-off tenants' workspaces are censused yet never written (gateOff), and flipping the tenant flag off mid-run halts that tenant at the next batch boundary. One mechanism is both the tenant selector and the 15 §2.1 abort flag; batch knobs are env (`CHANNEL_BACKFILL_BATCH_SIZE`/`_BATCHES_PER_TICK`) | Fail-closed choice recorded (backfill only ever runs where dual-write is already live — exactly 05's ordering); amend 05's S-CH3 flag row at next touch to name the selector |
| 2026-07-06 | **S-CH3 provenance label MINTED — `source='backfill'`:** 05 §1.1's `source` grammar (`import:<src>` \| `provider:<p>` \| `user_edit` \| `reveal` \| `master`) has no label for the S-CH3 projection pass, and the true origin of a pre-channel flat value is unknowable at backfill grain. Minted `backfill` (no CHECK constrains the column; the label states the projection pass honestly rather than fabricating an origin). Same honesty posture: `type='other'` (05 §1.4's default — the flat slot carried no usage context; the import path's `work` would be an invention), `source_import_id=NULL` (the flat slot kept no lineage pointer), `first_seen_at`=insert time (the true first-seen is unrecoverable) | Recorded; add `backfill` to 05 §1.1's grammar list at next touch |
| 2026-07-06 | **S-CH3 decrypt helper + division of labour:** the phone decrypt runs IN-WORKER via core's `decryptPii` (`packages/core/src/import/encryptPii.ts` — `encryptPii`'s shipped counterpart, the same helper the reveal/re-verify paths use); derivation reuses the S-CH2 `buildPhoneChannelValue` (DM1: shipped `toE164`/`blindIndex`/`encryptPii`, zero new parsers), so `value_enc` stays the flat bytes verbatim and `raw_original_enc` stays NULL (value_enc IS the flat original — 05 §1.3's "only when it differs"). The repo's `backfillContactChannels` takes bytes only — no crypto in packages/db, per the S-CH2 layering ruling above | Consistent with the S-CH2 home/layering drift row; nothing to amend |
| 2026-07-06 | **S-CH4 per-tenant flag MINTED — `channels_read`:** 05's S-CH4 row pins ONLY the env half (`CHANNEL_READ_FROM_CHILD`, used verbatim) and reserves the per-tenant `feature_flags` canary "if wanted"; the phase brief mandates the dual gate at cutover (the house rollout posture, 01 §7.3; the S-CH2 `channels_dual_write` mint precedent), so `channels_read` is minted + seeded off in 0060 (no `_enabled` suffix, matching `channels_dual_write`). The composed gate is `CHANNEL_READ_FROM_CHILD` env AND the full S-CH2 dual gate AND `channels_read` — read IMPLIES dual-write (05 §5 ordering: a cutover atop an unmaintained cache is unsound), fail-closed at every layer | Recorded (fc3956e); amend 05 §Rollout at next touch to name both halves + the implication |
| 2026-07-06 | **S-CH4 gate is CALLER-evaluated; repos take an `opts.channelsFromChild` (no flag reads in packages/db):** 05 §5 never says WHERE the read gate is read. Shipped mirrors the S-CH2 posture: the SearchPort provider evaluates ONCE per port build; `runImport` ONCE per run; `getRevealedContact` IN-TX per read; `bulkRevealExport` ONCE per export — all via core's `isChannelReadFromChildEnabled`/`channelReadFromChildEnabledForScope` (env-off ⇒ zero queries, fail-closed ⇒ flat). The repositories (`searchRepository`, `contactRepository`, `contactChannelRepository` read family) take a boolean opt only, so packages/db stays flag-free (the house rule) and every gate-off call is byte-identical | Code shape wins; note for the 05 §5 amendment |
| 2026-07-06 | **S-CH4 dedup rung reads CHILD-ONLY gate-on (the 05 §6 dual-write-era UNION not built):** 05 §6 says "during dual-write (pre-cutover) the lookup UNIONs flat + child to see pre-backfill rows; after S-CH4 it reads child only." Shipped: the retarget is gated by the READ gate (`channelsFromChild`), which IMPLIES backfill completeness = 0 (the flip precondition), so gate-on the rung reads `contact_emails.blind_index` ONLY — correct because every primary is already backfilled. The pre-cutover flat∪child UNION (a dual-write-era dedup ENHANCEMENT, independent of the read gate) is deliberately NOT built here — it would belong to an S-CH3-era slice, not the cutover | By design (the read gate's precondition makes the UNION unnecessary); amend 05 §6 to scope the UNION to the pre-cutover enhancement |
| 2026-07-06 | **S-CH4 dedup retarget wired in `runImport` ONLY — `draftPreview` + `bulkProcessChunk` deferred:** the email-rung child probe is threaded through `findByDedupKeys`/`findByDedupKeysBatch` (opt default off ⇒ byte-identical) but only the LIVE sync-import path (`runImport`) evaluates + passes the gate. `buildDraftPreviewSummary` takes `(tx, workspaceId, …)` with NO tenantId in-signature (evaluating the composed gate needs tenantId — a signature ripple to its callers), and `bulkProcessChunk` is the double-dark COPY path (`BULK_IMPORT_ENABLED` off, and its S-CH2 dual-write was never instrumented either — the standing S-CH2 scope drift). Consequence gate-on: the preview's wouldCreate/wouldUpdate + the COPY dedup would read FLAT while the sync import reads CHILD — a preview/copy-vs-sync secondary-dedup parity gap, dark until the read flag flips | Deferred: preview retarget rides a `buildDraftPreviewSummary` tenantId-threading; COPY retarget rides the 05 §6 staging `channels` extension (the same S-CH2 (a) scope drift). Both pass byte-identical off today |
| 2026-07-06 | **S-CH4 UI-slice boundary — channel UI + user verbs are doc 04's, NOT this cutover:** S-CH4 delivers only the READ CONTRACTS (masked `channels` summaries, the `revealedContactSchema` `emails`/`phones` arrays, the export additional-value columns) that doc 04's channel API (add/edit/make-primary/remove — 05 §API/§UI) and doc 11's contact-detail surface will consume. No add/edit/promote/delete verb, no four-state channel list, no per-call phone picker ships here (05 §UI/§API defer them to 04/11); the `channel_*` audit actions (0058 CHECK) stay written-by-nobody until those verbs land | Boundary recorded; the read contracts are stable for the doc-04 slice to build against |
| 2026-07-06 | **S-CH4 05 under-specification hit — export "additional" column names + summary-schema home:** (a) 05 §5 names "a semicolon-joined 'additional' column" for export but no column name — minted `additionalEmails`/`additionalPhones` (gate-on additive; primary stays in `email`/`phone`). (b) the masked summary schemas + usage-type enums MOVED `contactChannels.ts` → `contacts.ts` because `maskedContactSchema` now embeds them and `contactChannels.ts` imports `contacts.ts` (keeping them there would cycle) — the `@leadwolf/types` barrel uses `export *`, so the surface is unchanged and NO consumer moved (the S-I7 leaf-placement precedent). (c) the `company` facet COUNT grouping stays on the flat primary domain gate-on (05 §5's any-value domain applies to the FILTER, which IS cut over; grouping by any-value changes row cardinality — the production-engine facet model is doc 12/G24 ◇) | (a)/(b) code shape wins, no doc edit; (c) recorded — amend 05 §5 to split the any-value FILTER (shipped) from the facet COUNT model (doc 12) |
| 2026-07-06 | **S-CH3 degenerate-contact posture — skip loud, never wedge, never half-write:** contacts that cannot be projected (phone ciphertext failing decrypt; a flat `email_blind_index` with NULL `email_enc`/`email_domain` — legacy-only states) are skipped per-contact BEFORE any insert for that contact (counted `backfill_skipped_total`) rather than aborting the batch — deliberately: they stay in the completeness count, so the S-CH4 gate stays honestly blocked and visible on the `backfill_remaining` gauge instead of the whale-wedge 15 §2.1 warns about. Out-of-vocabulary legacy grades are sanitized per 05 §Edge (`line_type` outside the union → `'unknown'`; an out-of-vocab `phone_status` → NULL — the flat columns have no DB CHECK, the child columns do) and counted | Posture recorded; a nonzero steady `backfill_remaining` after fleet convergence = manual data triage, by design |
| 2026-07-06 | **S-CH4 GATE-ON CONSISTENCY GAP (verifier escalation - dark today, MUST close before any channels_read flip):** SearchPort.searchContacts consumes the child-presence predicates gate-on, but searchRepository.countContacts / resolveVisibleIds / searchContactsTx callers (bulkActions.ts select-all count + id-resolve, reveal/routes.ts resolveVisibleIds, lists.ts dynamic-list member resolution) pass no read-opts and stay FLAT gate-on. With the flag flipped, a has_email/domain-filtered page could disagree with "Select all N" and dynamic-list membership. Byte-identical gate-off; structural (gate eval needs scope threading into 3+ callers) | CLOSED (S-CH4b — commit 1 `ed8bc69` threads the gate; commit 2 adds the itests). All three flat siblings now evaluate the SAME composed gate and pass `{channelsFromChild}`: `bulkActions.searchCount` + `resolveVisibleSelection` (widened to take tenantId, all 9 call sites), `reveal/routes.ts` select-all `resolveVisibleIds` (in-tx eval), `lists.ts` `resolveDynamicMembers`/`createDynamicList`/`listLists`. `facetCounts` + `suggest` stay flat by design (drift (c) — no channel-presence predicate). Minor rider FOLDED: `getRevealedContact` now fetches child ciphertext only for OWNED channels (wasteful-not-leaky). Byte-identical + zero-query gate-off (env short-circuit); the read-cutover itest proves page↔count↔resolve↔dynamic-membership agreement on a child-only-presence fixture gate-on + flat gate-off. Gates (typecheck/itests) CI-owed |
| 2026-07-06 | **05 §6 uniformity — a SECOND (title-canon) resolve/page divergence surfaced while closing S-CH4b, left as-is:** the page (`searchPortProvider.searchContacts`), `bulkActions` (resolve + count), and `lists` all run `expandTitleFilters` (core title taxonomy) before the repo, but `reveal/routes.ts`'s select-all-matching `resolveVisibleIds` passes `criteria` RAW. So a bulk-reveal "Select all" over a `title=CEO` search resolves the un-expanded ids — FEWER than the page showed (which also matched "Chief Executive Officer"). Orthogonal to channels_read (present flag-off too); NOT touched by S-CH4b (channel scope only) to keep the change byte-identical + reviewable | Recorded — reveal-route title-canon expansion is a one-line follow-up (import `expandTitleFilters` at the apps/api edge, as `bulkActions` does); belongs with the bulk-reveal select-all slice, not the channel cutover |
| 2026-07-06 | **S-CH5 env-pair gating + NO per-tenant flag MINTED:** 15/05 name S-CH5 as "job off" with no flag detail. Shipped fail-closed like S-CH3: a NEW env `CHANNEL_RECONCILE_ENABLED` gates registration together with `CHANNEL_DUAL_WRITE` (a reconcile is meaningful only where child rows are maintained). Deliberately NO new per-tenant flag — the sweep REUSES `channels_dual_write` (tenant select + batch abort) and the composed READ gate (`CHANNEL_READ_FROM_CHILD` + `channels_read`) for the direction, so it inherits the exact S-CH2/S-CH4 rollout state per tenant with zero new control surface. Batch knobs are env (`CHANNEL_RECONCILE_BATCH_SIZE`/`_BATCHES_PER_TICK`) | Fail-closed choice recorded (reconcile only runs where dual-write is live; direction only flips where reads flipped); amend 05's S-CH5 row + §Rollout at next touch to name the enable + that it reuses the existing gates |
| 2026-07-06 | **S-CH5 phase rule reads the flags PER BATCH, in-tx (05 §3.4 "the job never guesses"):** the repair direction is not a job-payload choice — every batch tx first evaluates `isChannelDualWriteEnabled` (tenant select + abort, fail-closed ⇒ gateOff) then `isChannelReadFromChildEnabled` (read OFF ⇒ flat wins; read ON ⇒ child wins). A mid-run per-tenant read-gate flip takes effect on the next batch, so the sweep is always repairing in the tenant's CURRENT phase — the §R-P3 cutover-reversal drill's "repair direction flipped back to flat-wins" is automatic. Same in-tx eval mechanics as S-CH2/S-CH4 | Code shape wins (the flags are the single source of the phase); note for the 05 §3.4 amendment |
| 2026-07-06 | **S-CH5 drift metrics name-encode the direction:** 05/15 want a direction-labelled `channel_drift_repaired_total`; the shipped zero-dep metrics renderer (`apps/workers/src/metrics.ts`) has NO label support (name→value maps), so the direction is encoded in the name — `leadwolf_channel_drift_repaired_flat_total` / `_repaired_child_total` (+ `drift_detected_total`, `drift_skipped_total`, and the `drift_remaining` gauge). The backfill_* family set the same name-encoding precedent | Faithful zero-dep equivalent of a labelled counter; swapping in a real Prom client later only changes the render, not the call sites (the module's stated contract) |
| 2026-07-06 | **S-CH5 flat-wins BUILT payloads REUSE the S-CH3 backfill primitive (CH-INV-1 single-pathed):** the flat→child re-projection bytes are produced by `planContactChannelBackfill` verbatim (email ciphertext/blind-index copied, never re-encrypted; phone decrypt→`toE164`→sanitize in-worker) and creates route through `backfillContactChannels` — so a flat-wins repair writes byte-for-byte what the backfill would have, keeping the "one canonical projection" discipline. The reconcile-specific writes (demote/promote/write-primary/project-to-flat) are thin repo primitives the core runner sequences (demote-then-promote under the non-deferrable `uniq_*_primary`, 05 §2.1) | Consistent with the S-CH3 layering (bytes-only repo, crypto in core); nothing to amend |
| 2026-07-06 | **S-CH5 degenerate/vacuum handling is a SUPERSET of S-CH3 backfill + the non-representable-grade phantom:** a "flat present, no child" contact (05 §Edge degenerate-1) is drift the reconcile ALSO repairs (via the backfill primitive), so where enabled S-CH5 subsumes any residual backfill tail. Skip-loud reuses S-CH3's posture (undecryptable phone / incomplete flat email → counted, never wedged). The one non-convergent case flat-wins: an out-of-vocabulary legacy `phone_status`/`line_type` on the flat column (contacts has no DB CHECK; the child does) — the sanitized BUILT already equals the child, so the flat-wins write is a no-op (`builtEqualsPrimary` guard ⇒ no `updated_at` churn) and the raw divergence stays visible as steady `drift_remaining` for manual triage; CHILD-wins CLEARS it (the flat adopts the child's sanitized grade). Same "nonzero steady gauge after burn-in = manual triage, by design" posture as the S-CH3 degenerate row | Posture recorded; a persistent small `drift_remaining` after fleet convergence flat-wins is expected for such legacy rows — child-wins (post-cutover) drains it |
| 2026-07-06 | **S-C7 SUBSUMED by S-CH4** (no rebuild). 04's S-C7 ("masked-DTO channel summary + read projection §6") already shipped by S-CH4: `maskedContactSchema.channels` (`contactChannelSummariesSchema`, `types/contacts.ts:104,491`) + the read projection (`toMaskedContact`/`withChannelSummaries`/`channelSummariesForContacts`), wired at `searchRepository` + `contactRepository` masked list/detail, gate-optional-populated. Shipped shape is 05 §5's richer per-value form — a SUPERSET of 04 §6's `{count,primaryStatus}` sketch (04 §6 is "shapes only", defers to 05). The masked-contract test rides S-CH4's readcutover itest | No rebuild; 04 S-C7 + 15 seq 51 flipped to SUBSUMED |
| 2026-07-06 | **S-C8 SUBSUMED by S-CH5** (no rebuild). 04's S-C8 ("cache↔child reconcile sweep, count metric + gated repair; pairs with S-CH5") checks the SAME invariant S-CH5 holds permanently — CH-INV-1 (flat ≡ live `is_primary` child). S-CH5 already handles the merge-facing promotion/demotion/vacuum states (`swap_to_flat`/`promote_oldest_then_project_flat`/`write_primary_from_flat` — `channelReconcile.ts:63–129`), provides the count gauge (`leadwolf_channel_drift_remaining` → S2) + gated repair, never retires, and reads the same phase flags 04 §pre-build/§Rollout named. Merge (S-C4) is Phase 4 — no merge sequences exist in Phase 3 to need a separate sweep; a Phase-4 merge's atomic primary-swap drift is caught by the same permanent sweep. **T4 (cache↔child invariant, §T-P3 ruling M3) is covered by S-CH5's flat-wins/child-wins/degenerate/idempotent itests** | No rebuild; 04 S-C8 + 15 seq 49 flipped to SUBSUMED |
| 2026-07-06 | **S-C6 markers are TRANSIENT by design.** The `duplicate_of_contact_id` suggestions S-C6 writes are re-derived wholesale by the dedup sweep (`prospect/dedup.ts` `clearDuplicateFlags`→`flagDuplicates`, which recomputes name@@domain markers ONLY — it does not know the phone/collision class). So a next dedup run clears them. The DURABLE signal home is I5 `match_links` (04 §3.5); S-C6 writes the immediate suggestion GUARDED (`markDuplicateSuggestion` = only-when-null + non-self ⇒ never clobbers the sweep's marker or a stronger signal) | Accepted; durable home is I5 match_links, out of this series' scope |
| 2026-07-06 | **S-C6 bulk-staging equivalent BLOCKED.** "+ bulk-staging equivalents" (04 S-C6 / 15 seq 50) has nothing to hook into: the dark bulk COPY chunk (`bulkProcessChunk.ts`) is NOT channel-instrumented (the shipped S-CH2 drift — it never calls `applyChannelWrite`, and its `findByDedupKeysBatch` omits the read-gate opt). Built the SYNC `runImport` path only — mirroring S-CH2/S-CH4's own sync-only scope | Rides the deferred bulk COPY channel instrumentation; note for that step |
| 2026-07-06 | **S-C6 email-collision branch is FORWARD-WIRING.** The cross-key EMAIL collision → suggestion (05 §2.2) is unreachable under SINGLE-email import with the read gate on: the email dedup rung (S-CH4) resolves any live value to its owner FIRST, so `applyChannelWrite` never returns `collision`. Wired + correct now (fires when the multi-email mapping increment 15 seq 52 / `additionalEmails` lands). The T7 itest covers the REACHABLE phone-signal path end-to-end | Recorded; the branch goes live at seq 52 |
| 2026-07-06 | **S-A3 `account_locations` has NO `source_import_id` — 06 §3 vs 07 §3 (RULING: 07 wins).** 06 §3 says locations carry the provenance trio "as §1" (which includes the `source_import_id` FK), but 07 §3's FK inventory lists ONLY tenant/workspace + account_id for `account_locations` (and 07 §2.2's ER draws the `source_imports→account_domains` edge but NONE to `account_locations`) — two independent, explicit omissions in the consolidation doc. Ruled per 07 §1 (07 is the after-the-fact consistency authority): locations get `source` + `pinned` (plain columns, not contradicted) but NO lineage FK. Coherent with 06 §3's own "locations are subordinate to identity, NEVER a dedup key" — lineage matters for the domains match-ladder, not for subordinate firmographics | 07 §3 governs; amend 06 §3's "provenance trio, as §1" to "source + pinned (no lineage FK)" at next touch. A per-location lineage column is a cheap additive migration if import ever needs it |
| 2026-07-06 | **S-A4 `root_account_id` has NO FK (07 §2.2 explicit).** 06 §2 leaves it unspecified; 07 §2.2 draws it FK-less (`\|o..o{`) — it is a denormalized recomputed ultimate-parent CACHE (family key = COALESCE(root, id)), and an FK would add referential overhead on every subtree root-recompute. Implemented bare-uuid. §7's "every new overlay child table carries real FKs" is not violated: root is a self-denormalization column ON accounts, not a child-table pointer | 07 §2.2 governs; recorded, no doc edit |
| 2026-07-06 | **S-A4 composite parent FK uses PG15+ COLUMN-TARGETED `SET NULL (parent_account_id)` (06/07 under-specified).** 06 §2 / 07 §3 say "SET NULL on purge" without the nuance that a plain `SET NULL` on the composite `(workspace_id, parent_account_id)` FK would try to null the NOT NULL `workspace_id` and ERROR. Resolved with the PG15+ column-list form (PG16 confirmed via `itestDb.ts`), nulling ONLY parent_account_id. The Drizzle mirror (`foreignKey(...).onDelete("set null")`) can only express the plain form — the hand-written 0061 is authoritative; the mirror carries a comment | Implemented + recorded; the drizzle-emitted plain form is a known mirror limitation (hand-written migration wins). Amend 06 §2 / 07 §3 to name the column-list form at next touch |
| 2026-07-06 | **S-A4 `uniq_accounts_ws_id` is a UNIQUE CONSTRAINT, not a bare unique index (FK-target mechanics).** 07 §4.1 calls it "(full unique)"; a composite FK requires its referenced columns to back a UNIQUE/PK CONSTRAINT (a `CREATE UNIQUE INDEX` alone is NOT a valid FK target in Postgres). Implemented via `ALTER TABLE … ADD CONSTRAINT … UNIQUE` (drizzle `unique()`, not `uniqueIndex()`) so the composite parent FK resolves | Mechanics note; no doc edit (07 §4.1's intent is satisfied) |
| 2026-07-06 | **S-A5 domain-unique swap: drop-then-recreate under the SAME name in 0061 vs CONCURRENTLY online in prod.** 07 §4.1 / 06 S-A5 specify an ONLINE swap ("create-new → drop-old"). The migration runs inside the drizzle migrator's txn (where `CONCURRENTLY` is illegal) and on fresh/empty CI DBs, so 0061 does `DROP INDEX … ; CREATE UNIQUE INDEX … (WHERE domain IS NOT NULL AND deleted_at IS NULL)` under the stable name `uniq_accounts_ws_domain` (keeps the Drizzle mirror name stable). The true zero-downtime online swap (CREATE UNIQUE INDEX CONCURRENTLY <tmp> → DROP CONCURRENTLY → RENAME) is documented in-migration as the PRODUCTION execution note. Behaviour-neutral today (deleted_at always NULL until the S-A5 write path) | Recorded; prod ops runbook uses the CONCURRENTLY form. The predicate add is inert until dual-write writes deleted_at |
| 2026-07-06 | **06-family DDL bundled into ONE migration (0061) across four sequenced steps (S-A1/S-A3/S-A4/S-A5).** 15 §M-SEQ sequences these as seq 53/56/57/58 with backfill/dual-write GATES between them, but those gates order DATA/write-path steps, not DDL — all four DDL slices are additive dead schema (07 §8), so bundling the schema substrate is safe and matches the DDL-first Phase-4 order ("06 family → 04 DDL → merge → UI"). EXCLUDED (next task): S-A1/S-A3 backfill, seq 54 S-A2 dual-write, seq 55 backfill re-run, seq 59 S-A6 read cutover + C2 rung. NO audit-action CHECK extension (ruling M1: the DDL steps write no action; coarse account.create/.update/.delete already exist) and NO retention-class seeds (07 §8/15 assign none to the 06 family — the deliberate contrast with S-CH1/0058) | Recorded; the write-path/ladder steps are the next task's train. Snapshots hand-stopped at 0028 (drizzle regen CI-owed) as for every migration since 0029 |
| 2026-07-07 | **S-A2 latent (verifier note, unreachable in the dark import-only scope):** in domainUpsert, an insert_primary losing the one-live-primary race to a concurrent same-account tx with a DIFFERENT domain re-reads by domain, finds nothing, and reports collision - dropping that domain instead of retrying as secondary. Same-run rows are sequential today and enrichment/manual account writers are not yet instrumented, so no reachable path | Revisit when the enrichment/manual account-domain writers land (their instrumentation slice); mirrors the shipped channel precedent |
| 2026-07-07 | **P5 fired-job ATTRIBUTION — created_by = the schedule's creator, NOT NULL/system.** The `scheduledImports.ts` schema comment (08 §9) pins a fired `import_jobs` row's `created_by` to NULL ("system/automation") with the schedule pointer in options. The fire-seam spec instead sets `created_by = row.createdByUserId` (the grant executed AS them) so the shipped import-complete notification path (which skips creator-less jobs) reaches the creator; the schedule pointer STILL rides `options.scheduleId`. `input.importedByUserId` is set to the creator too (fast completion notify) | Deliberate — creator-attribution enables completion notifications; amend the 08 §9 / schema comment to name creator-attribution + `options.scheduleId` at next touch |
| 2026-07-07 | **P5 disable NOTIFICATION type — reuses `'system'` (no dedicated value).** The closed `notificationType` enum (`reply_received`/`low_credits`/`import_complete`/`dsar_update`/`system`) has no `scheduled_import_disabled` value. The sweep's grant-loss / max-failures disable notification uses `type:'system'` with `entityType:'scheduled_import'` + the schedule id, idempotent via `existsForEntity` (a redelivery/second tick no-ops; a re-enable→re-disable of the SAME schedule dedups on the entity key and won't re-notify — accepted for v1) | Recorded; mint a `scheduled_import_disabled` type + a dedicated migration/CHECK when the P5 notification surface is designed (doc 11) |
| 2026-07-07 | **P5 AUDIT gap — schedule create/update/delete are LOG-ONLY (structured console line), no `audit_log` row.** The shipped audit-action CHECK (0054/0057) carries no `scheduled_import.*` action and this slice adds no migration (ruling M1: extend the CHECK once per phase, with a migration). A DB audit INSERT would fail the CHECK at write time | The P5 audit actions land with the P5 audit migration (M1 precedent); until then the console line is the record. Escalation: mint `scheduled_import.{created,updated,deleted,fired,disabled}` in the next P5 migration train |
| 2026-07-07 | **P5 over-fast-pair FIRE POSTURE — treated as a FIRE FAILURE, not routed to copy (copy-mode scheduled fires DEFERRED).** v1 fires the fast lane only. At fire time `decideImportRouting(copyEngaged:false)` throws `ImportTooLargeError` for an over-threshold stored object ⇒ `recordFailure` (advance next_run_at, auto-disable at 5). Symmetrically, the create verb REFUSES an over-fast-pair template up front. A schedule whose source grows past the fast pair between fires self-disables rather than silently degrading | Deferred: the copy-drive scheduled fire (store-object → drive kind on the unified queue) is future P5 work; the sweep's fire path + the create refusal both name it |
| 2026-07-07 | **P5 LIST-VISIBILITY choice — GET list is member+, MUTATE is creator∪elevated.** Doc 08 §9 / the 0063 types don't pin the read matrix. A schedule is workspace AUTOMATION CONFIG (not a PII job), so LIST returns all workspace schedules to member+ (10 §2 config posture), while PATCH/DELETE are creator∪elevated. A member who can SEE a schedule but is not its creator gets 403 on mutate (not 404 — it is not hidden from them); a foreign/absent id is 404 (RLS + the workspace check) | Recorded; confirm against the P5 UI surface (doc 11) when it lands — tighten to creator-only list if schedules ever carry PII in the read DTO (they do not today — non-PII by construction) |
| 2026-07-07 | **P5 SOURCE model — stored-object branch ONLY (no connected-source/remote-URL).** 08 §9 pins two source branches (cron+connected-source OR re-uploaded template file); v1 ships the STORED-OBJECT branch only (a schedule re-imports a FileStore object uploaded at create). The connected-source/remote-URL branch is DEFERRED — it takes URLs+credentials bound by 13 §8's SSRF forward-guard (deny-by-default egress, allowlist, the shipped `ssrfGuard`). v1 fetches NO outbound URL, so it opens NO SSRF surface | Deferred; the connected-source branch's acceptance criteria = 13 §8 SSRF guard reuse. The schema/types headers already record this |
| 2026-07-07 | **P5 CADENCE model — interval ENUM (hourly/daily/weekly), not a cron parser.** 08 §9 says "cron"; v1 ships an interval enum (pure, dependency-free, unit-testable cadence math — `computeNextRunAt` skip-missed-windows) stored in a varchar+CHECK column so a cron string can be added later without a rename | Recorded (schema/types headers carry it); amend 08 §9 to name the enum-first v1 + the forward path at next touch |
| 2026-07-07 | **P5 sweep grant read — `getRoleForUser` opens a NESTED scoped tx while holding the fire row's FOR UPDATE lock.** The per-schedule fire tx locks the row (`getByIdForUpdate`) then reads the creator's live role via `workspaceRepository.getRoleForUser`, which opens its OWN `withTenantTx` (a second pooled connection) — a bounded nested acquisition. Safe because the sweep is leader-locked + serial (one schedule at a time ⇒ ≤2 connections), and the object read/parse also happens under the lock (low-volume, bounded to the fast pair) | Accepted for v1 (few schedules, serial sweep); if schedule volume grows, read the role in-tx via a direct query (the repo would gain a tx-aware `getRoleForUserInTx`) |
| 2026-07-07 | **S-A6 BUILT DARK (15 §M-SEQ seq 59) — read cutover + ladder rung C2 + hierarchy cycle guard, triple-inert.** The account read cutover ships behind the composed read gate; the import company-match rung C2 activates gate-on; the overlay projection + cycle guard land as gated/no-verb primitives. Flag-off is byte-identical (proven: the C2 probe is skipped ⇒ C1-only, zero `account_domains` queries) and cost-identical (env-off short-circuit). Migration number taken: **0064** (the P5 scheduled-imports sibling took 0063 — journal monotonic, verified before write) | Recorded; the enable is a CI-green + backfill-converged (`countAccountsMissingDomainChild = 0`) flip, per 15 seq 55's precondition edge |
| 2026-07-07 | **S-A6 read-gate MINTED — `account_read_from_child` + env `ACCOUNT_READ_FROM_CHILD`.** 06 §Rollout names "a per-tenant dual-gate (named at PR time)" for S-A6 but pins NO flag key; 15 seq 59 says the same. Minted here mirroring the S-CH4 `channels_read` + `CHANNEL_READ_FROM_CHILD` pair (and the S-A2 `account_domains_dual_write` mint precedent), seeded off in 0064. The composed gate is `ACCOUNT_READ_FROM_CHILD` env AND the FULL S-A2 dual gate (`ACCOUNT_DOMAINS_DUAL_WRITE` + `account_domains_dual_write`) AND `account_read_from_child` — **read IMPLIES dual-write** (06 §4 ordering: a cutover atop an unmaintained cache is unsound), fail-closed at every layer. The gate is CALLER-evaluated in core (`accountRead.ts`); packages/db stays flag-free (the S-CH4 house rule) | Recorded; amend 06 §Rollout at next touch to name both halves + the implication |
| 2026-07-07 | **S-A6 rung C2 = SECONDARY-ONLY probe (`is_primary=false`) — the "C1 miss ⇒ probe" reading of 06 §5.** `findAccountIdBySecondaryDomain` filters `is_primary=false`, so C1 (a domain that is some account's flat primary) AND the create-on-miss path both stay on the shipped atomic `upsertByDomain` (INSERT…ON CONFLICT — its name-refresh/master-bridge semantics + concurrency-safety unchanged); C2 intercepts ONLY the genuine secondary-domain match `upsertByDomain` alone would miss (its flat cache holds a DIFFERENT primary ⇒ it would mint a duplicate, G17). ≥2 hits impossible by `uniq_account_domains_ws_domain` (whole-set live unique) ⇒ LIMIT 1 is exact. A C2 hit resolves + refreshes the account (`refreshMatchedAccount`, mirroring C1's DO UPDATE set) and NEVER moves/re-primaries the domain (06 §1/§Edge) | Faithful to 06 §5's C1-as-fast-path split; the minimal-behavior-change reading (only true secondaries change outcome gate-on). No doc edit |
| 2026-07-07 | **S-A6 latent CORRECTNESS fix (gate-INDEPENDENT) — `upsertByDomain`/`upsertByDomainBatch` ON CONFLICT `targetWhere` now `domain IS NOT NULL AND deleted_at IS NULL`.** S-A5's 0061 swapped `uniq_accounts_ws_domain` to the live-only partial (`… AND deleted_at IS NULL`); Postgres infers a partial-unique arbiter ONLY when the ON CONFLICT predicate IMPLIES the index predicate, so the pre-S-A5 `domain IS NOT NULL` alone no longer matches and would FAIL inference at runtime (`no unique or exclusion constraint matching…`) on the very first import once 0061 is applied — a break the S-A5 DDL train introduced but did not close. Fixed in commit 1 (not commit 2's soft-delete slice) so the C2 itest's own gate-off arm passes in CI. Behaviour-neutral today (deleted_at always NULL); becomes 06 §4's "import on a tombstoned account mints a NEW account" once the delete verb writes deleted_at | **Escalation:** verify in CI — this is the decisive gate for ALL imports post-0061, not just S-A6. Recorded as a latent-bug closure |
| 2026-07-07 | **S-A6 rung C3 (name+country) = ABSENT from the write path by design (06 §5).** C3 is REVIEW-ONLY — never a silent import key. The import ladder ships C1+C2 only; a domainless / no-C1/C2 row creates a new account exactly as today. The C3 review-suggestion surface (duplicate-review queue with the `root_account_id` same-family hint) has no substrate in this series (mirrors the S-C6 warning-band posture — no review-queue writer shipped) | By design; the C3 suggestion surface rides doc 11's duplicate-review slice, same as S-C6's markers |
| 2026-07-07 | **S-A6 cycle guard = repo method + tests, NO API verb (06 §2 write-path, doc 04/11 owns the verb).** `setParentAccount` + `AccountHierarchyError` implement the full 06 §2 write-time validation (FOR-UPDATE endpoint locks in id order; depth-capped recursive ancestor walk ⇒ `self_parent`/`cycle`/`depth_cap`; same-tx `root_account_id` recompute for the moved node + its WHOLE subtree, attach + clear). No `PATCH /accounts/:id` parent verb ships — 06 §API defers the account-edit surface to doc 04/11; the guard lands as the gated primitive its verb will call, itest-proven. The self-parent DB CHECK + composite same-workspace FK (0061) are the DB backstops | No-verb seam recorded; the parent verb rides doc 04/11's account UI slice (same boundary as S-CH4's channel verbs → doc 04) |
| 2026-07-07 | **S-A6 overlay read projection = gated primitive, NO consuming route yet.** `overlayExtensionsForAccounts` (batched, no-N+1, live domains[]/locations[] DTO-shaped, RLS-walled) is the S-A6 account-detail read projection (06 §API). There is NO shipped account-DETAIL endpoint returning `AccountSchema`+overlay (only the account-search GRID, whose `MaskedAccount` keeps the flat `domain` unchanged per 06 §API) — so the projection ships as the gate-on primitive the future detail route/UI consumes, itest-proven (own-workspace resolve + cross-workspace non-leak). NON-PII: domains/addresses are carried clear (the deliberate contrast with S-CH4's masked channel summaries) | No-consumer seam recorded; the account-detail route + drawer ride doc 04/11 (same slice as the verbs) |
| 2026-07-07 | **S-A6 tombstone read exclusion applied GATE-INDEPENDENTLY + BEHAVIOUR-NEUTRALLY (06 §4).** Nothing writes `accounts.deleted_at` yet (no delete verb shipped), so `deleted_at IS NULL` is identically true for every row ⇒ results unchanged; implemented UNCONDITIONALLY (not behind the read flag) as the correct forward semantics where 06 §4 pins it: `accountSearchRepository` search/count/facets/suggest ALL carry it (page↔count↔facet agreement — the S-CH4b lesson), and `searchRepository`'s 5 contact-search account leftJoins filter tombstones so a tombstoned company renders company-less. The dedup C1 (`upsertByDomain` live-only arbiter) + C2 (`is_primary=false AND deleted_at IS NULL` probe) already exclude tombstones | By design (06 §4 is unconditional); becomes load-bearing when the delete verb lands (doc 04/11). Itest asserts the exclusion with a hand-tombstoned row |
| 2026-07-07 | **S-A6 rung C2 wired in `runImport` ONLY — bulk COPY path NOT instrumented (mirrors S-CH4/S-C6 scope).** The C2 probe is threaded through the LIVE sync-import path only; `bulkProcessChunk` (the double-dark COPY path, `BULK_IMPORT_ENABLED` off) calls `upsertByDomainBatch` and is NOT account-child-instrumented (it never dual-writes `account_domains`), so it has no C2 hook — the exact standing S-CH2/S-C6 bulk scope drift applied to accounts. The `upsertByDomainBatch` targetWhere fix (above) DOES cover it (gate-independent correctness). Gate-on, a COPY import would miss C2 secondaries the sync path catches — dark until BOTH flags flip AND copy is engaged | Deferred: the bulk COPY C2 rung rides the same staging-instrumentation slice as S-CH2 (a)/S-C6's bulk-blocked equivalent |
| 2026-07-07 | **S-A6 units deferred to itests (no genuinely pure new logic).** The composed gate is a boolean AND of async flag reads (DB-bound); the C2 probe, cycle-guard walk, and overlay projection are all DB-shaped. The pure account-domain-plan decisions already have `accountChildPlan.test.ts`. So S-A6's proofs live in two new itests (`accountLadder.readcutover.itest.ts` — C2 on/off byte-identity, overlay RLS non-leak, tombstone exclusion; `accountHierarchy.cycleguard.itest.ts` — self/2-cycle/deep-cycle/depth-cap/root-recompute) — CI-owed (no bun in the sandbox) | Honest: no pure unit added because there is no pure new unit; the itests are the gate |
| 2026-07-07 | **S-C1 shipped — additive merge-supersession columns (04 §1, migration 0065).** `contacts.merged_into_contact_id` (self-FK, SET NULL on survivor hard-purge, mirroring `duplicate_of_contact_id`) + `merged_at` (timestamptz) + partial `idx_contacts_merged_into` on the tombstoned-loser set. DEAD SCHEMA — nothing writes them until the S-C4 engine lands AND the S-C3 dual gate is ON; behaviour-neutral/byte-identical until then (§R-P4: DOWN = drop, safe while flag-off wrote nothing). DISTINCT from `duplicate_of_contact_id` (reversible suggestion) by design (04 §1 "Why a new pointer") — a merged loser may carry both; `merged_into` is the authoritative irreversible hop | By design; the columns are the substrate the S-C4 executor's loser-tombstone write (04 §3.4) + the 410-style detail read (04 §API) consume |
| 2026-07-07 | **S-C2 shipped — P4 audit-action CHECK extension + scalar-edit metadata contract (04 §4, migration 0066).** `contact.merge` added to `auditAction` (types, source of truth) + the `audit_log_action_enum` CHECK (schema + 0066, the 0057/0058 drop-and-re-ADD technique with the full current list). Written by nobody until S-C4+S-C3. The `FieldChangeAuditMetadata` contract (`{ fields:{name:{b,a}}, src }`, clear-text scalars only, never channel values) is minted in types and WIRED into `editContactFields`: an in-tx `contact.update` audit row with before/after for the changed CONTACT_PROVENANCE_FIELDS (04 §4's "every scalar edit audits in-tx"). The SAME contract is the substrate the S-C4 merge event reuses | Behaviour note: `editContactFields` now emits an audit row (additive observability, ungated) + early-returns on an empty edit set (was a pointless fieldProvenance re-write). CI-owed: any existing editContact itest that asserted zero audit rows must gain the `contact.update` expectation |
| 2026-07-07 | **S-C3 shipped — contact-merge dual gate seeded off (04 §3.1, migration 0067).** `CONTACT_MERGE_ENABLED` env kill-switch (env.ts, explicit-"true"-only, mirroring CHANNEL_DUAL_WRITE) + per-tenant `contact_merge_enabled` flag (0067 seed, off/off, surfaces in the generic flag console) + the composed evaluator `isContactMergeEnabled`/`contactMergeEnabledForScope` (core, mirroring channelDualWrite.ts; fail-closed — a gate it cannot read is OFF because merge is destructive+irreversible). `CONTACT_MERGE_FLAG_KEY` minted in `@leadwolf/types/contactMerge.ts` so api/workers never drift. Flag-off ⇒ verb 404s, engine never constructed, byte-identical current behavior | By design; the gate the S-C4 engine + S-C5 verb + S-C9 wrapper all ride. §R-P4: flipping off halts NEW merges, never rolls back executed ones (no unmerge) |
| 2026-07-07 | **P5 DELTA / incremental imports (08 §9 layer 3) BUILT DARK — external_id upsert rung + `modified_since` = the shipped content-hash skip.** 08 §9 pins three delta layers: (1) row-grain `content_hash` idempotent-skip **already shipped** on `source_imports` (`uniq_source_imports_ws_content`, an internal merge-core optimization — "no platform exposes hashing as UX"), (2) upsert-on-declared-key = the shipped S-I6 merge triad, and (3) *new*: "an `external_id` upsert option (map a column as the caller's stable key; needs a per-workspace `(workspace_id, external_id)` unique — schema sketch only)" + "a `modified_since` filter for connected sources". Only layer 3 is new work. SHIPPED (dark, triple-gated): additive `contacts.external_id` varchar(255) + partial unique `uniq_contacts_ws_external_id` (per-workspace, LIVE rows only — mirrors the three shipped identity uniques; migration **0068**); the `externalId` canonical field + `canonicalContactRowSchema` + a conservative `headerAliases` entry (specific aliases only, **never a bare "id"**); `DELTA_IMPORTS_ENABLED` env kill-switch + `delta_imports_enabled` flag (0068 seed) + `deltaImportsGate.ts` (mirrors scheduledImportGate, fail-closed, zero queries env-off); `runImport` gains the external-id rung as the **TOP dedup precedence** (probed before the email→linkedin→sales-nav ladder; falls through on a miss) + a fill-blank external_id stamp on landing; `prepareContact` derives it; `runFastImport` + the api route thread the `externalIdUpsert` option (honored only when the client opts in AND the gate is on). **THREE-way inert:** env off OR flag off OR the option absent ⇒ the shipped ladder, byte-identical (the engine never reads or writes `external_id`). Proven: `importDelta.itest.ts` (precedence-holds-identity-across-email-change vs gate-off-forks-a-new-contact; content-hash re-skip; external_id stays NULL gate-off) + `delta.test.ts` (auto-map + prepareContact pure legs) | The subsystem-status row 38 ("Incremental / connected-source / API imports 🔲") flips to 🌒 for the **external-id delta half** (connected-source + API-push branches stay 🔲/deferred). **CI-owed:** bun test/typecheck/biome + the itest (no bun in sandbox); drizzle regen (journal hand-stopped, see below). Enable = the dual gate flip once CI green |
| 2026-07-07 | **P5 delta `modified_since` = DELIVERED by the shipped content-hash skip; the timestamp-CURSOR variant DEFERRED to connected sources.** 08 §9's `modified_since` is scoped "for connected sources" with the honest caveats "cursors beat timestamps and deletes are invisible to deltas (periodic full re-sync)". Connected sources are DEFERRED (scheduled-imports v1 ships the stored-object branch only; CRM-pull is out of scope — the 08 §9 / P5 scheduled rows). For the STORED-OBJECT / scheduled re-import case the delta-skip is ALREADY delivered by the shipped `source_imports.content_hash` idempotency (a row unchanged since a prior fire lands `skipped` — actually workspace-wide, BROADER than "for that schedule/source", which is a strict superset of the delta intent). So no new `modified_since` code ships: a timestamp column/cursor for a connected-source substrate that does not exist would be a half-built invention. The itest's content-hash-re-skip leg is the delta-skip proof | Faithful to 08 §9's honest-caveat framing. The timestamp-cursor `modified_since` lands WITH the connected-source branch (bound by 13 §8 SSRF forward-guard, same deferral as scheduled-imports' remote-URL branch); amend 08 §9 to name "layer-1 content-hash skip already serves the stored-object delta case" at next touch |
| 2026-07-07 | **P5 delta external-id rung PRECEDENCE + the fill-blank WRITE decision.** 08 §9 makes `external_id` "the caller's stable key" → implemented as the TOP dedup rung (Salesforce upsert-on-external-id: the caller declared THIS is the record's identity, so it is probed BEFORE the email→linkedin→sales-nav ladder — `externalMatch ?? findByDedupKeys(...)`). The key is written FILL-BLANK only (`setExternalId … WHERE external_id IS NULL`): a new insert gets it, a row matched BY external_id already holds it (no-op), a row matched by EMAIL whose contact holds a DIFFERENT external key keeps the stored one (08 §9 "conflict strategy defers to the field_provenance winner-map + pin" — we never silently clobber a declared key). CAVEAT (doc-16, thin honest version): an import row STILL needs an intrinsic identity key (email/linkedin/sales-nav) to LAND — the overlay has no external-id-ONLY contact (prepareContact throws), so external_id is an ADDITIONAL top-precedence match key, not a replacement identity. A pure external-id-only upsert would need an ER/identity change out of this slice's scope | By design; recorded as the honest bound. Amend 08 §9 to name "external_id is an additional top rung atop the intrinsic-identity requirement" at next touch. A cross-key conflict (declared key disagrees with a populated one) could later feed the G21 review queue (same home as S-C6 markers) |
| 2026-07-07 | **P5 delta kept OUT of `contactRepository` — a dedicated `contactExternalIdRepository` (concurrency isolation).** The external-id read+write pair (`findIdByExternalId` + fill-blank `setExternalId`) ships as a SEPARATE repo, not folded into `contactRepository`, and `external_id` is stamped via a dedicated UPDATE rather than a new `ContactWriteValues` field — deliberately, so the delta slice never edits the contact-repo surface the concurrent Phase-4 merge train (S-C1…S-C9) owns. RLS/grants unchanged (the new repo reads/updates `contacts`, which `leadwolf_app` already writes). `runImport`'s ROW-level dedup is the only engine seam touched | Isolation choice recorded; the separate repo is additive and the merge train's `contactRepository`/`contactMergeRepository` edits never collide with it |
| 2026-07-07 | **P5 delta external-id rung wired in `runImport` (sync/fast) ONLY — copy + scheduled fires NOT instrumented (mirrors S-CH2/S-C6/S-A6 bulk scope).** The `externalIdUpsert` option rides the FAST lane; the api route does NOT pass it to `submitOneShotCopy` (the copy path), and the scheduled-import sweep does not carry it through `options` yet. So an over-threshold (copy) delta import or a scheduled fire runs the shipped ladder, NOT the external-id rung — the exact standing bulk-scope drift applied to delta. Gate-on + copy-engaged, an external-id upsert would silently fall back to the email ladder | Deferred: the copy-path external-id rung rides the same staging-instrumentation slice as S-CH2 (a)/S-A6's bulk-blocked equivalent (`bulkProcessChunk`); the scheduled-fire delta rides `scheduledImportRepository.options` (an additive jsonb key, no schema change) when picked up. Both dark today |
| 2026-07-07 | **P5 delta MIGRATION-NUMBER race — took 0068 (siblings held 0065/0066/0067 for the merge train); journal regen CI-owed.** At creation time the shared checkout held committed 0065 (`contact_merge_columns`) + 0066 (`contact_merge_audit_action`) and an uncommitted 0067 (`seed_contact_merge_flag`), so this migration took **0068** (`contact_external_id_delta`) — the next free on disk (journal monotonic; verified right before write). ~~Per the branch's standing convention … this migration adds the SQL file WITHOUT a journal entry~~ **CORRECTED 2026-07-07 (merge-verifier finding):** that rationale was FALSE — only SNAPSHOTS are CI-regen'd; `meta/_journal.json` is hand-maintained (0053–0064 all carry entries; drizzle's migrator applies ONLY journaled migrations, so an unjournaled file silently never applies — the 0060 defect class). The merge verifier registered 65/66/67 (`49a20e0`); the coordinator registered 68 directly. All 65–68 are journaled monotonic | CLOSED — journal complete through 0068; snapshots alone remain CI drizzle-regen. The 0068 number is safe |
| 2026-07-07 | **P5 delta SHARED-INDEX/checkout race — commit-2 code landed under a sibling's commit.** The shared git index (multiple agents, one working copy) meant a concurrent sibling `git commit` captured this slice's STAGED commit-2 files (engine + gate + repo + route + barrels) into the sibling's `beac866` ("docs(01) … map regen") commit; the substrate landed cleanly as `19d906e` (commit 1). All delta files are present + coherent on the branch tip (verified: `runImport` external rung + stamp, `deltaImportsGate`, `contactExternalIdRepository`, route plumbing, `prepareContact`, `headerAliases`, `runFastImport`, both barrels). Attribution is misplaced (a "docs" commit carries feat code) but the code is complete and correct | Recorded as a shared-checkout artifact (same class as env.ts sweeping earlier). No code impact; noted for the human's commit-log read. The wizard/auto-map UI EXPOSURE of the `externalId` field is deferred to a doc-11 slice (the field is mappable-by-contract, dark in the web wizard) |
| 2026-07-07 | **S-C4 shipped — the core merge ENGINE (04 §3, the ONE value-moving merge — DM1).** Pure planner `contactMergePlan.ts` (field union via `planFieldWrite`/`planUserEdit`, never SQL CASE: default = survivor-wins/loser-fills-blanks; pinned survivor scalar structurally immune; explicit loser pick re-pins; custom_fields survivor-wins union) + unit test. Tx executor `contactMergeRepository.ts` (db): FOR-UPDATE lock in id order (concurrent-merge race guard), RLS-scoped load (IDOR guard), survivor writes + §1 blank-fills (account/owner/master) + reveal-trio adoption + last_activity max, type-aware channel demotion, the FULL Class-A re-point inventory, loser tombstone (PII+identity-keys nulled → keys released), and the `contact.merge` audit event returning its id. Orchestrator `mergeContact.ts` (core) composes them in one withTenantTx; legality → ValidationError(self)/NotFoundError(IDOR)/ContactMergedError(409+mergedInto)/ContactMergeCapError(409 daily cap, env CONTACT_MERGE_DAILY_CAP). Merge itest covers T1/T2/T3/T5/T6 (CI-owed — no bun) | FINAL Class-A re-point inventory (all covered): contact_emails, contact_phones (collision-collapse), list_members (union), source_imports, activities, record_tags (G23 dedupe), contact_reveals (never-double-charge collapse), suppression_list(match_type='contact_id'), consent_records, outreach_log (union), email_thread/message/event, sales_nav_links, scores, intent_signals, + duplicate_of markers aiming at the loser. Class-B untouched (import/enrichment/reveal_job_rows, provider_calls). NO 07 §3.1 omission — all named tables present |
| 2026-07-07 | **S-C5 shipped — the merge VERB + preview + DTOs (04 §API/§6).** DTOs minted in `@leadwolf/types/contactMerge.ts` (mergeRequest/Result/Preview + MergeFieldDecision, closed decidable-field allowlist = CONTACT_PROVENANCE_FIELDS). Own route file `features/contacts-merge/` (per the concurrency pin — NOT in any import route): `POST /contacts/:id/merge` (survivor=:id; Idempotency-Key; requireRole member+; dual-gated 404-off via contactMergeEnabledForScope) + `GET /contacts/:id/merge-preview?loser=` (read-only field matrix + child-impact counts via `previewContactMerge` + `countLoserChildren`). Mounted at /api/v1/contacts (no path overlap with reveal/scores/activities). **S-C5 review-queue ACT ruling:** the "accept a duplicate suggestion" path IS this merge verb (survivor=canonical, loser=flagged dup) — the existing contacts-dedup route stays dismiss-only; the accept BUTTON is doc 11/S-U8. **dedup×tombstone verified (no fix needed):** `listForDedup` excludes `deleted_at` so a merged loser drops out of marker re-derivation automatically; `clearDuplicateFlags` clearing a tombstoned loser's stale pointer is a harmless no-op; the engine re-points markers AIMING at the loser in-tx (no dangling) | Verb-level gate-off-404 + idempotent-replay are api-integration tests (CI-owed, no bun); engine-level IDOR/replay proven in contactMerge.itest.ts (T2/T5). Role matrix downgrade-to-member is doc 10's; shipped default = member+ (reveal money-verb family) |
| 2026-07-07 | **S-C9 BUILT (not just ruled) — Surface-1 maker-checker true-merge wrapper over the SAME engine (04 §3.5).** New `contact_true_merge` approval operation (types/dataApproval.ts: enum + DATA_APPROVAL_OPERATIONS + `contactTrueMergeParamsSchema` reusing mergeFieldDecisionSchema) wired end-to-end into the shipped maker-checker dispatch (apps/api admin `POST /approvals/:id/approve`): the executor branch calls core `runStaffContactMerge` on the TARGET tenant's withTenantTx (RLS-correct — NEVER the owner path), riding the tenant merge gate + maker≠checker. The grain-A `execDedupMerge` is UNCHANGED (marker-only, reversible) — `contact_true_merge` supersedes it FOR VALUE-MOVING only (04 §3.5: the executor is never extended to move values itself). **SEAM CAVEAT:** the engine commits in its own tenant tx, so it is NOT rolled back by a later failure in the owner-path approval tx; `runStaffContactMerge` makes a re-approve after a partial SAFE by treating an already-merged-into-this-survivor loser as an idempotent success (not a wedging 409). One merge implementation (DM1), two entry surfaces (customer verb S-C5 + this) | Built; the imperfect cross-tx atomicity is inherent to composing a tenant-scoped engine under an owner-path approval — resolved by idempotent replay, documented here. The Surface-1 approval-flow itest (15 seq 65) is CI-owed |
| 2026-07-07 | **P5 delta RETRY-FAILED gap CLOSED (adversarial review) — the `externalIdUpsert` opt-in now survives a retry-failed child.** The gap: `externalIdUpsert` rode only the BullMQ payload, never the durable `import_jobs` row, so the S-I10 retry-failed verb (which RECONSTRUCTS the child `ImportFastInput` from the persisted parent row + ledger `input`) silently DROPPED it — a partially-failed delta import's retried rows would fall back to the email→linkedin→sales-nav ladder instead of the external-id TOP rung, forking contacts the parent would have upserted. Fix (routes.ts, ~14 lines): the submit path persists `{ externalIdUpsert: true }` into the job `options` jsonb (createJob already accepts `options`; omitted when false ⇒ options stays `{}`, byte-identical); the retry verb reads `parent.options.externalIdUpsert` and RE-HONORS it on the child ONLY after re-evaluating the DELTA gate IN-TX (`isDeltaImportsEnabled(tx, tenantId)`, fail-closed via the env short-circuit) — never trusting the persisted flag, so a gate flipped off between parent and retry drops the option — then threads it onto both the child payload and the child's own `options` (a retry-of-a-retry chain re-honors each link). BullMQ auto-retries were already safe (same payload); the reaper never re-drives fast jobs (rows-in-payload unrecoverable, Phase A) so only the S-I10 reconstruction path had the gap | Closed in code (not drift-logged). Copy-lane + scheduled-fire delta stay deferred (row 248) — this only closes the FAST retry-reconstruction path. Also fixed in the same review: a self-contradictory `findIdByExternalId` doc comment that claimed a `deleted_at` live-filter the query does not have (behavior is correct ladder-parity — it mirrors `findByDedupKeys`, which also matches tombstones — only the comment was false) |
| 2026-07-07 | **S-U8 pair-list DTO gap — the queue renders NAMES + added-date only.** The shipped `DuplicatePairView` (`packages/types/src/dedupReview.ts`) carries only `{duplicateId, duplicateName, duplicateCreatedAt, canonicalId, canonicalName}`. 11 §5.1 wants each queue row to also show primary email domain, a match-reason chip (email / LinkedIn / name+domain / ER-proposed), a "same corporate family" chip (`root_account_id`), the import-file source link, and age filters. NONE of those fields exist on the pair-list DTO, so the upgraded `DuplicatesSection` renders duplicate name · kept-as name · added date · Review · Not-a-duplicate — and the rest is noted here, not invented. The merge PANEL's per-field comparison DOES render fully (the `mergePreviewSchema` DTO carries the 7 masked scalars + pin flags + child-impact) | Defer: the queue-chrome fields need the pair-list DTO widened (a `04`/`06` list-endpoint follow-up); the merge review itself is complete against the shipped preview contract |
| 2026-07-07 | **S-U8 bulk-resolve DEFERRED (defer-with-drift, the honest read of 11 §5.1).** 11 §5.1 pins **bulk DISMISS** (multi-select "Not a duplicate") and DELIBERATELY withholds bulk merge (merge is human-only, 2-record cap, 04 §3.6). The backend has NO batch verb for EITHER: `unmark` and `merge` are both one-id-per-call. The shipped `@leadwolf/ui` `DataTable` also exposes no multi-select UI. Shipped for S-U8: single per-pair Review→Merge + single Dismiss (unchanged). Bulk multi-select dismiss (sequential N `unmark` calls behind a selection UI) is DEFERRED | Defer: bulk dismiss lands when `DataTable` gains a selection primitive (§8.3-adjacent DS gap) + a progress affordance; bulk merge stays permanently un-offered per 04 §3.6 |
| 2026-07-07 | **S-U8 Company-match tab DEFERRED entirely.** 11 §5.1's second `Tabs` panel — the `ambiguous_company_match` review queue (06 §5 C3 + ambiguity rows) — has no shipped apps/web-reachable list/resolve endpoint (06's S-A read cutover + C2 rung are unbuilt, doc-16 row "account_domains…"). Only the **Possible duplicates** tab is upgraded; no second tab is added | Defer: the company-match tab lands with 06's ambiguous-match resolution API (06 §5) |
| 2026-07-07 | **S-U8 merge 404 conflation — gate-off vs deleted-record.** The merge preview/verb 404 is treated UNIFORMLY as `MergeNotEnabledError` (the dual gate is dark → hide Merge, keep dismiss-only). A gate-ON 404 from a contact deleted mid-review (11 §8.4's "Already handled" empty state) therefore also reads as "not enabled". Acceptable (both are rare + fail safe toward dismiss-only), but the distinct "Already handled" state is NOT rendered | Defer: needs the merge routes to distinguish a gate-404 from a missing-record-404 via a problem `type` slug (the UI switches copy on the slug, never on `detail` text — 11 §API) |
| 2026-07-07 | **S-U8 lazy gate detection (no probe endpoint).** There is no dedicated merge-gate capability signal for apps/web, so the Review/Merge affordance renders OPTIMISTICALLY and hides across the queue on the FIRST preview 404 (`onNotEnabled` → section state), rather than being hidden pre-render. In a gate-off workspace a user can click Review once and get the honest "Merging isn't enabled yet" drawer state before the affordance disappears | Acceptable dark (branch undeployed; gate seeded off/off); a session capability flag or a HEAD-style gate probe would hide Merge pre-click. Mirrors the S-U2 not-enabled pattern (which also learns from a 404) |
| 2026-07-07 | **S-U8 TanStack-in-a-useState-slice (deliberate, non-big-bang).** The NEW merge surface is TanStack-shaped (`duplicateKeys` factory + `useMergePreview`/`useMergeContacts`, mirroring `features/import/keys.ts` + `useImportMutations.ts`; the global `QueryClientProvider` is live since S-U1). The pair LIST stays the shipped `useDuplicatePairs` useState hook — the dismiss path is byte-untouched (11's in-place mandate). Per 11 §Reconciliation ("existing hooks migrate only as their surfaces are rebuilt"), the list is not force-migrated | Deliberate; the list hook migrates to TanStack if/when the queue gains keyset pagination + filters (the §5.1 chrome that also needs the DTO widening above) |
| 2026-07-07 | **S-U8 DS focus-trap gap (packages/ui, not this tree).** 11 §7.2 mandates DS focus-trap + return-focus on the merge Drawer/Dialog; the shipped `@leadwolf/ui` `Drawer`/`Dialog` do Esc-to-close + scrim-dismiss + `role=dialog`/`aria-modal` + a labelled close button, but do NOT trap or return focus. The merge panel adds `aria-label`s on every field's radio group + the drawer title; keyboard reach + Esc work | DS-owed (the §8.3 focus-trap primitive lives in `packages/ui`, out of the S-U8 tree — a concurrent verifier owns it); not hand-rolled in the feature |
