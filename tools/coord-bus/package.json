{
  "name": "coord-bus",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Coordination bus — a remote MCP server that lets multiple Claude Code terminals share a task board and nudge each other. Developer tooling; not part of the LeadWolf product build.",
  "scripts": {
    "start": "bun run server.ts",
    "start:node": "tsx server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.0.0",
    "zod": "^3.25.0"
  }
}
