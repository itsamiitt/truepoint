// @leadwolf/forge-capture-sdk — the envelope-v2 builder + content-hash + size/PII guards shared by the MV3
// extension and the forge-api ingest validation (docs/planning/forge/07, re-homed from @forge/capture-sdk).
// THIN: imports ONLY @leadwolf/types (it ships into the untrusted extension process — see the
// forge-capture-sdk-stays-thin dependency-cruiser rule). Ported in P3.
export const FORGE_CAPTURE_SDK_VERSION = "0.0.0";
